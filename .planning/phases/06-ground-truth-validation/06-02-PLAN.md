---
phase: 06-ground-truth-validation
plan: 02
type: execute
---

<objective>
Add comparison utilities and reporting methods to Validator.

Purpose: Enable batch validation of multiple clustering approaches and generate human-readable reports.
Output: Validator.compare() working, report generation methods, integration with Visualizer.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./06-02-summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan in this phase:
@.planning/phases/06-ground-truth-validation/06-01-PLAN.md

# Existing source to reference:
@wavelength_analysis/ground_truth_validation.py

# Target module:
@spectral_select/validation.py

# Visualizer for integration:
@spectral_select/visualizer.py

**Tech stack available:** numpy, pandas, sklearn.metrics, scipy.optimize
**Established patterns:** Factory methods, property-based accessors, DataFrame outputs for comparisons
**Constraining decisions:**
- Phase 05-03: Visualizer already has plot_confusion_matrix, plot_per_class_metrics, plot_accuracy_heatmap, plot_roi_overlay
- Phase 06-01: ValidationMetrics and GroundTruth types defined
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Validator.compare() for batch evaluation</name>
  <files>spectral_select/validation.py</files>
  <action>
Implement the compare() method on Validator class:

def compare(
    self,
    clustering_results: Dict[str, np.ndarray],
    ground_truth: Union[np.ndarray, GroundTruth],
    valid_mask: Optional[np.ndarray] = None
) -> pd.DataFrame:
    """
    Compare multiple clustering results against ground truth.

    Args:
        clustering_results: Dict mapping method names to cluster label arrays
        ground_truth: Ground truth labels (array or GroundTruth instance)
        valid_mask: Optional boolean mask for valid pixels

    Returns:
        DataFrame with columns: Method, N_Clusters, Purity, ARI, NMI, AMI,
        V-Measure, Homogeneity, Completeness, FM-Score, sorted by Purity descending.
    """

Implementation:
- Iterate over clustering_results items
- For each: call self.fit(labels, ground_truth, valid_mask)
- Extract metrics into row dict
- Build DataFrame with columns in specified order
- Sort by Purity descending
- Reset self._metrics to None after comparison (allow fresh fit() calls)
- Return DataFrame

Also add get_metrics_dict() method to Validator that returns metrics as flat dict (for DataFrame row construction).
  </action>
  <verify>python -c "
import numpy as np
from spectral_select import Validator

# Create synthetic data
gt = np.random.randint(0, 3, (10, 10))
c1 = np.random.randint(0, 3, (10, 10))
c2 = np.random.randint(0, 4, (10, 10))

v = Validator()
df = v.compare({'Method A': c1, 'Method B': c2}, gt)
print(df.columns.tolist())
print(len(df))
"</verify>
  <done>compare() returns sorted DataFrame with all metrics columns, works with multiple clustering inputs</done>
</task>

<task type="auto">
  <name>Task 2: Add report generation methods</name>
  <files>spectral_select/validation.py</files>
  <action>
Add reporting methods to Validator:

1. generate_report(self, output_path: Optional[Path] = None) -> str:
   - Requires fitted Validator
   - Produce Markdown-formatted report with:
     * Header with timestamp
     * Summary statistics (n_clusters, n_gt_classes, key metrics)
     * Cluster-to-GT mapping table
     * Per-class metrics table (precision, recall, F1)
   - If output_path provided, write to file
   - Return report string

2. to_json(self, path: Path) -> None:
   - Save metrics to JSON file
   - Use ValidationMetrics.to_dict()

3. Add ValidationMetrics class method from_json(path: Path) -> ValidationMetrics
   - Load and reconstruct ValidationMetrics from JSON

Import logging at module level, use logger.info/debug for progress messages.
  </action>
  <verify>python -c "
import numpy as np
from spectral_select import Validator

gt = np.random.randint(0, 3, (10, 10))
clusters = np.random.randint(0, 3, (10, 10))

v = Validator()
v.fit(clusters, gt)
report = v.generate_report()
print(report[:200])
"</verify>
  <done>generate_report() produces Markdown string, to_json() saves metrics, from_json() loads them back</done>
</task>

<task type="auto">
  <name>Task 3: Add Visualizer integration helper</name>
  <files>spectral_select/visualizer.py</files>
  <action>
Add classmethod to Visualizer for creating validation-focused visualizer:

@classmethod
def from_validation(
    cls,
    validator: "Validator",
    cluster_map: np.ndarray,
    output_dir: Optional[Path] = None
) -> "Visualizer":
    """
    Create Visualizer configured for validation plots.

    Args:
        validator: Fitted Validator with computed metrics
        cluster_map: 2D cluster label array
        output_dir: Directory for saving plots

    Returns:
        Visualizer instance ready for validation plotting
    """

The method should:
- Check validator.is_fitted, raise ValueError if not
- Extract ground_truth array from validator (add _ground_truth private attribute to Validator in fit())
- Create Visualizer instance with:
  * cluster_map stored in _validation_cluster_map
  * ground_truth stored in _validation_ground_truth
  * metrics accessible via _validation_metrics
  * output_dir set
- These attributes enable plot_confusion_matrix, plot_accuracy_heatmap, plot_roi_overlay to work

Also update Validator.fit() to store ground_truth reference for later retrieval.

Add property to Validator:
@property
def ground_truth(self) -> Optional[np.ndarray]:
    return self._ground_truth
  </action>
  <verify>python -c "
import numpy as np
from spectral_select import Validator, Visualizer

gt = np.random.randint(0, 3, (10, 10))
clusters = np.random.randint(0, 3, (10, 10))

v = Validator()
v.fit(clusters, gt)
viz = Visualizer.from_validation(v, clusters)
print('Visualizer created:', viz is not None)
"</verify>
  <done>Visualizer.from_validation() creates configured instance, validation plots accessible</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Validator.compare() returns DataFrame with all expected columns
- [ ] Validator.generate_report() produces Markdown string
- [ ] Validator.to_json() writes file, ValidationMetrics.from_json() reads it
- [ ] Visualizer.from_validation() creates valid instance
- [ ] No import errors or circular dependencies
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Validator integrates cleanly with Visualizer
- Reports are human-readable
- Phase 6 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/06-ground-truth-validation/06-02-SUMMARY.md`:

Include:
- All accomplishments
- Files modified
- Decisions made
- Phase 6 completion status
- Next Phase Readiness for Phase 7 (Notebook Migration)
</output>
