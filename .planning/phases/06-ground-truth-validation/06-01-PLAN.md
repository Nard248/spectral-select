---
phase: 06-ground-truth-validation
plan: 01
type: execute
---

<objective>
Implement Validator class with clustering metrics computation API.

Purpose: Provide a clean interface for evaluating clustering results against ground truth, using sklearn-compatible metrics.
Output: Working Validator class with fit(), score(), and metrics accessors.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./06-01-summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/05-visualization-module/05-03-SUMMARY.md

# Existing source to refactor:
@wavelength_analysis/ground_truth_validation.py

# Target module:
@spectral_select/validation.py

# Types available from Phase 3:
@spectral_select/types.py

**Tech stack available:** numpy, pandas, sklearn.metrics (ARI, NMI, V-measure, etc.), scipy.optimize.linear_sum_assignment
**Established patterns:** dataclass with __post_init__ validation, scikit-learn estimator pattern (fit/transform), property-based accessors
**Constraining decisions:**
- Phase 02-01: Protocol-based component interfaces with @runtime_checkable
- Phase 04-01: scikit-learn-style API (fit/transform/get_wavelengths)
- Phase 05-03: Visualizer handles all plotting (confusion matrix, accuracy heatmap already implemented)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ValidationMetrics and GroundTruth data types</name>
  <files>spectral_select/types.py, spectral_select/__init__.py</files>
  <action>
Add two new dataclasses to types.py:

1. GroundTruth dataclass:
   - labels: np.ndarray (2D integer array, -1 for background)
   - color_mapping: Dict[int, Tuple[int, int, int, int]] (label → RGBA)
   - class_names: Optional[List[str]] (human-readable names per label)
   - n_classes property (excluding background)
   - valid_mask property (labels >= 0)
   - from_array classmethod for simple array input
   - to_dict/from_dict for serialization

2. ValidationMetrics dataclass:
   - adjusted_rand_score: float
   - normalized_mutual_info: float
   - adjusted_mutual_info: float
   - fowlkes_mallows_score: float
   - v_measure: float
   - homogeneity: float
   - completeness: float
   - purity: float
   - cluster_to_gt_mapping: Dict[int, int]
   - confusion_matrix: np.ndarray
   - per_class_precision: Dict[int, float]
   - per_class_recall: Dict[int, float]
   - per_class_f1: Dict[int, float]
   - n_ground_truth_classes: int
   - n_predicted_clusters: int
   - to_dict/from_dict for JSON serialization
   - summary() method returning formatted string

Export both from __init__.py.
  </action>
  <verify>python -c "from spectral_select import GroundTruth, ValidationMetrics; print('OK')"</verify>
  <done>Both dataclasses importable from package root, to_dict/from_dict work correctly</done>
</task>

<task type="auto">
  <name>Task 2: Implement Validator class with metrics computation</name>
  <files>spectral_select/validation.py</files>
  <action>
Replace placeholder with full implementation:

1. Validator class with sklearn-style API:
   - __init__(self): Initialize empty (no config needed)
   - fit(cluster_labels, ground_truth, valid_mask=None) -> self:
     * Accept np.ndarray or GroundTruth for ground_truth
     * Flatten and filter arrays (remove background -1)
     * Compute all sklearn metrics (ARI, NMI, AMI, V-measure, etc.)
     * Compute purity via _calculate_purity_and_mapping
     * Build confusion matrix via sklearn.metrics.confusion_matrix
     * Compute per-class precision/recall/F1 via _calculate_per_class_metrics
     * Store results in self._metrics (ValidationMetrics instance)
     * Return self for chaining
   - score() -> float: Return adjusted_rand_score (primary metric)
   - metrics property -> ValidationMetrics: Return computed metrics (raise if not fitted)
   - is_fitted property -> bool: Check if metrics computed
   - compare(results_dict: Dict[str, np.ndarray], ground_truth) -> pd.DataFrame:
     * Evaluate multiple clustering results against same ground truth
     * Return sorted DataFrame (by purity) with all metrics

2. Private helper methods (ported from ground_truth_validation.py):
   - _calculate_purity_and_mapping(cluster_labels, ground_truth) -> Tuple[float, Dict]
   - _calculate_per_class_metrics(confusion_matrix) -> Dict with precision/recall/f1

Use imports from sklearn.metrics: adjusted_rand_score, normalized_mutual_info_score, adjusted_mutual_info_score, fowlkes_mallows_score, v_measure_score, homogeneity_score, completeness_score, confusion_matrix.

Do NOT include any visualization code - Visualizer already handles plotting.
  </action>
  <verify>python -c "from spectral_select import Validator; v = Validator(); print('is_fitted:', v.is_fitted)"</verify>
  <done>Validator instantiable, fit() computes metrics, score() returns float, metrics property returns ValidationMetrics</done>
</task>

<task type="auto">
  <name>Task 3: Add ground truth loading utility</name>
  <files>spectral_select/validation.py</files>
  <action>
Add load_ground_truth_from_png function (module-level, not class method):

def load_ground_truth_from_png(
    png_path: Union[str, Path],
    background_colors: Optional[List[Tuple[int, int, int, int]]] = None,
    target_shape: Optional[Tuple[int, int]] = None,
    class_colors: Optional[Dict[str, Tuple[int, int, int]]] = None,
    color_tolerance: int = 50
) -> GroundTruth:

Port logic from ground_truth_validation.py extract_ground_truth_from_png, but:
- Return GroundTruth instance instead of tuple
- Replace print statements with logging.debug/info
- Use warnings.warn for non-critical issues
- Handle edge cases (file not found, invalid image format)

Default background_colors: [(24, 24, 24, 255), (168, 168, 168, 255)]

Export function in __init__.py.
  </action>
  <verify>python -c "from spectral_select.validation import load_ground_truth_from_png; print(load_ground_truth_from_png.__doc__[:50])"</verify>
  <done>Function loads PNG annotations, returns GroundTruth with labels/color_mapping/class_names</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from spectral_select import Validator, GroundTruth, ValidationMetrics"` succeeds
- [ ] `python -c "from spectral_select.validation import load_ground_truth_from_png"` succeeds
- [ ] Validator.fit() returns self and populates metrics
- [ ] Validator.score() returns float (ARI)
- [ ] ValidationMetrics.summary() produces readable string
- [ ] No import errors or circular dependencies
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors (N/A - Python)
- Validator follows sklearn estimator pattern
- Types properly exported from package root
  </success_criteria>

<output>
After completion, create `.planning/phases/06-ground-truth-validation/06-01-SUMMARY.md`
</output>
