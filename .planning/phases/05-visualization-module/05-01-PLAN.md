---
phase: 05-visualization-module
plan: 01
type: execute
---

<objective>
Create the Visualizer class skeleton with public API and core infrastructure.

Purpose: Establish the foundation for all visualization methods with consistent styling, output directory management, and matplotlib configuration.
Output: Visualizer class with __init__, save utilities, and styling infrastructure ready for plotting methods.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior phase context:**
@.planning/phases/04-analysis-engine/04-04-SUMMARY.md

**Source files to reference:**
@spectral_select/visualizer.py
@spectral_select/types.py
@spectral_select/__init__.py
@wavelength_analysis/core/visualization.py
@wavelength_analysis/supervised_visualizations.py

**Key patterns from prior phases:**
- Config dataclass for settings (Phase 2)
- WavelengthResult, WavelengthBand types (Phase 3)
- Analyzer.fit() returns self (Phase 4)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Visualizer class skeleton with initialization</name>
  <files>spectral_select/visualizer.py</files>
  <action>
Replace the placeholder Visualizer class with full implementation:

1. Imports: matplotlib.pyplot, seaborn, numpy, pathlib.Path, typing, Optional, Union, List, Dict
2. Class docstring explaining purpose (publication-quality visualizations for wavelength analysis)
3. __init__ method:
   - output_dir: Optional[Union[str, Path]] = None (defaults to cwd/visualizations)
   - dpi: int = 300 (publication quality)
   - figsize: Tuple[int, int] = (12, 8) (default figure size)
   - style: str = "seaborn-v0_8-whitegrid" (clean scientific style)
   - Store as private attributes: _output_dir, _dpi, _figsize, _style
   - Create output directory if not exists
   - Apply matplotlib style in _setup_style() method
   - Initialize color palette as _colors from seaborn "husl"

4. Properties:
   - output_dir: Path (read-only)
   - dpi: int (read-only)
   - figsize: Tuple[int, int] (read-only)

5. Private methods:
   - _setup_style() -> None: Apply matplotlib style, set seaborn palette
   - _save_figure(fig, name: str) -> Path: Save figure to output_dir with dpi, return path, close figure
   - _get_color(index: int) -> str: Return color from palette by index (cyclic)

Match existing codebase patterns: private attrs with public properties (from Analyzer).
  </action>
  <verify>python -c "from spectral_select import Visualizer; v = Visualizer(); print(f'Output: {v.output_dir}')"</verify>
  <done>Visualizer class initializes with default settings, creates output directory, applies style</done>
</task>

<task type="auto">
  <name>Task 2: Add from_result and from_analyzer factory methods</name>
  <files>spectral_select/visualizer.py</files>
  <action>
Add class methods for convenient instantiation:

1. @classmethod from_result(cls, result: WavelengthResult, output_dir: Optional[Path] = None, **kwargs) -> "Visualizer":
   - Creates Visualizer bound to a specific result
   - Store result as _result attribute
   - If output_dir not provided, use f"visualizations/{result.sample_name}"
   - Pass **kwargs to __init__

2. @classmethod from_analyzer(cls, analyzer: "Analyzer", output_dir: Optional[Path] = None, **kwargs) -> "Visualizer":
   - Import Analyzer at runtime to avoid circular imports (TYPE_CHECKING pattern from config.py)
   - Creates Visualizer bound to an analyzer
   - Store analyzer as _analyzer attribute (can access analyzer.result_ after fit)
   - If output_dir not provided, use f"visualizations/{analyzer.config.sample_name}"
   - Pass **kwargs to __init__

3. Add TYPE_CHECKING import for Analyzer at top of file (like protocols.py pattern)

4. Add instance attributes (all Optional, set in factory methods):
   - _result: Optional[WavelengthResult] = None
   - _analyzer: Optional["Analyzer"] = None

5. Add property: has_result: bool (returns True if _result is set or _analyzer has result_)

This enables:
- Visualizer.from_result(result).plot_influence_heatmap()
- Visualizer.from_analyzer(analyzer).plot_all()
  </action>
  <verify>python -c "from spectral_select import Visualizer, WavelengthResult, WavelengthBand, AnalysisMetrics; import numpy as np; bands = [WavelengthBand(rank=i+1, excitation_nm=365.0, emission_nm=400.0+i*10, emission_band_index=i, influence_score=1.0-i*0.1) for i in range(3)]; result = WavelengthResult(sample_name='test', selected_bands=bands, metrics=AnalysisMetrics.from_bands(bands, 100)); v = Visualizer.from_result(result); print(f'Has result: {v.has_result}')"</verify>
  <done>Factory methods create Visualizer instances bound to result/analyzer with appropriate output directories</done>
</task>

<task type="auto">
  <name>Task 3: Add stub plotting methods with NotImplementedError</name>
  <files>spectral_select/visualizer.py</files>
  <action>
Add placeholder methods that will be implemented in subsequent plans. This establishes the public API:

1. Wavelength analysis plots (05-02):
   - plot_influence_heatmap(self) -> Path: "See Phase 5 Plan 02"
   - plot_wavelength_scatter(self) -> Path: "See Phase 5 Plan 02"
   - plot_excitation_distribution(self) -> Path: "See Phase 5 Plan 02"
   - plot_influence_ranking(self) -> Path: "See Phase 5 Plan 02"
   - plot_wavelength_coverage(self) -> Path: "See Phase 5 Plan 02"

2. Clustering/validation plots (05-03):
   - plot_confusion_matrix(self, cm: np.ndarray, class_names: Optional[List[str]] = None) -> Path: "See Phase 5 Plan 03"
   - plot_accuracy_heatmap(self, ground_truth: np.ndarray, predictions: np.ndarray) -> Path: "See Phase 5 Plan 03"
   - plot_roi_overlay(self, cluster_map: np.ndarray, roi_regions: List[Dict]) -> Path: "See Phase 5 Plan 03"

3. Summary method:
   - plot_summary_dashboard(self) -> Path: "See Phase 5 Plan 02"
   - plot_all(self) -> List[Path]: "See Phase 5 Plan 03" (calls all available plots)

Each method should raise NotImplementedError with message like:
  raise NotImplementedError("plot_influence_heatmap: See Phase 5 Plan 02")

This establishes the full API surface for notebooks/users to discover.
  </action>
  <verify>python -c "from spectral_select import Visualizer; v = Visualizer(); print(dir(v))" | grep plot</verify>
  <done>All stub methods defined with NotImplementedError, API surface complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from spectral_select import Visualizer"` succeeds
- [ ] Visualizer() creates output directory
- [ ] Visualizer.from_result() works with WavelengthResult
- [ ] All plot_* methods exist and raise NotImplementedError
- [ ] No import errors
</verification>

<success_criteria>
- Visualizer class has __init__ with output_dir, dpi, figsize, style
- Factory methods from_result and from_analyzer work
- Private styling infrastructure (_setup_style, _save_figure, _get_color) ready
- All public plot methods defined as stubs
- Pattern matches Analyzer class (private attrs, public properties)
</success_criteria>

<output>
After completion, create `.planning/phases/05-visualization-module/05-01-SUMMARY.md`
</output>
