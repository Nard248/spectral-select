---
phase: 05-visualization-module
plan: 03
type: execute
---

<objective>
Implement clustering and validation visualizations including confusion matrices, accuracy maps, and ROI overlays.

Purpose: Provide visualizations for evaluating clustering/classification performance against ground truth.
Output: Working plot_confusion_matrix, plot_accuracy_heatmap, plot_roi_overlay, plot_per_class_metrics, and plot_all methods.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visualization-module/05-02-SUMMARY.md

**Source files to reference:**
@spectral_select/visualizer.py
@wavelength_analysis/supervised_visualizations.py

**Original visualization patterns from SupervisedVisualizations:**
- plot_confusion_matrix(): Enhanced confusion matrix with counts and percentages
- plot_per_class_metrics(): Bar plot for precision/recall/F1 per class
- plot_accuracy_heatmap(): Spatial map showing correct/incorrect predictions
- plot_misclassification_patterns(): Per-class spatial accuracy maps
- plot_roi_overlay_with_accuracy(): ROI boxes overlaid on clustering result
- plot_metric_comparison(): Radar plot of overall metrics

**These visualizations work with:**
- Confusion matrices (np.ndarray)
- Ground truth arrays (2D np.ndarray, -1 for background)
- Prediction arrays (2D np.ndarray)
- ROI regions (List[Dict] with name, coords, color)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement plot_confusion_matrix and plot_per_class_metrics</name>
  <files>spectral_select/visualizer.py</files>
  <action>
Add classification/clustering evaluation visualizations:

1. plot_confusion_matrix(self, cm: np.ndarray, class_names: Optional[List[str]] = None,
                         title: str = "Confusion Matrix") -> Path:
   - Normalize for color mapping: cm / cm.sum(axis=1, keepdims=True)
   - imshow with 'Blues' colormap
   - Add text annotations: count and percentage in each cell
   - Use white text on dark cells, black text on light cells (threshold-based)
   - Colorbar: "Normalized Frequency"
   - Grid lines for readability
   - Axis labels: "Predicted Class", "True Class"
   - If class_names None, use "Class 0", "Class 1", etc.
   - Save as f"confusion_matrix_{title.lower().replace(' ', '_')}.png"

2. plot_per_class_metrics(self, per_class_metrics: Dict[int, Dict[str, float]],
                          title: str = "Per-Class Performance") -> Path:
   - Expects dict: {class_id: {'precision': float, 'recall': float, 'f1': float, 'support': int, 'class_name': str}}
   - Two-panel figure (2 rows, 1 column)
   - Top panel: Grouped bar chart for precision, recall, F1 per class
   - Add value labels on bars
   - Add horizontal line for average F1
   - Bottom panel: Bar chart of support (sample counts) per class
   - Title: "Class Distribution in Dataset"
  </action>
  <verify>python -c "from spectral_select import Visualizer; import numpy as np; v = Visualizer(); cm = np.array([[50, 5], [3, 42]]); print(v.plot_confusion_matrix(cm, ['A', 'B']))"</verify>
  <done>Confusion matrix and per-class metrics visualizations work correctly</done>
</task>

<task type="auto">
  <name>Task 2: Implement plot_accuracy_heatmap and plot_roi_overlay</name>
  <files>spectral_select/visualizer.py</files>
  <action>
1. plot_accuracy_heatmap(self, ground_truth: np.ndarray, predictions: np.ndarray,
                         title: str = "Spatial Accuracy") -> Path:
   - Create accuracy map: 0=incorrect, 1=correct, NaN=background (-1 in ground_truth)
   - Custom colormap: red for incorrect, green for correct
   - Calculate and display accuracy stats in title: "Accuracy: XX% (correct/total pixels)"
   - Colorbar with 'Incorrect' and 'Correct' labels
   - Save as "accuracy_heatmap.png"

2. plot_roi_overlay(self, cluster_map: np.ndarray, roi_regions: List[Dict],
                    ground_truth: Optional[np.ndarray] = None,
                    roi_metrics: Optional[Dict[str, Dict]] = None,
                    title: str = "ROI Overlay") -> Path:
   - roi_regions format: [{'name': str, 'coords': (y_start, y_end, x_start, x_end), 'color': '#RRGGBB'}, ...]
   - Build class_to_color mapping from ground_truth if provided (dominant class per ROI)
   - Create RGB image using ROI colors for each cluster
   - Draw rectangle borders around ROIs
   - Add labels with ROI name above each ROI
   - If roi_metrics provided, add accuracy percentage to labels
   - 3-panel figure: (1) clustering result, (2) ROI overlay, (3) ROI accuracy bar chart
   - Handle background pixels (cluster_map == -1) as white
  </action>
  <verify>python -c "from spectral_select import Visualizer; import numpy as np; v = Visualizer(); gt = np.array([[0,0,1,1],[0,0,1,1],[-1,-1,-1,-1]]); pred = np.array([[0,0,1,0],[0,0,1,1],[-1,-1,-1,-1]]); print(v.plot_accuracy_heatmap(gt, pred))"</verify>
  <done>Accuracy heatmap and ROI overlay visualizations work with spatial data</done>
</task>

<task type="auto">
  <name>Task 3: Implement plot_all and finalize Visualizer</name>
  <files>spectral_select/visualizer.py</files>
  <action>
1. plot_all(self) -> List[Path]:
   - Orchestrates all available visualizations
   - If has_result: call all wavelength plots (from 05-02)
   - Returns list of all generated file paths
   - Print summary: "Generated X visualizations in {output_dir}"
   - Handle exceptions gracefully: if one plot fails, continue with others and warn

2. Add helper method for consistent figure creation:
   - _create_figure(self, nrows: int = 1, ncols: int = 1,
                    figsize: Optional[Tuple] = None) -> Tuple[Figure, Axes]:
   - Uses self._figsize if figsize not provided
   - Returns fig, axes tuple for consistent subplot creation

3. Add convenience method:
   - save_all_to_pdf(self, filename: str = "all_visualizations.pdf") -> Path:
   - Collects all PNGs in output_dir and combines into single PDF
   - Uses matplotlib.backends.backend_pdf.PdfPages
   - Good for paper submissions

4. Final cleanup:
   - Ensure all methods have proper docstrings
   - Remove any remaining NotImplementedError stubs
   - Verify all imports are at top of file
  </action>
  <verify>python -c "from spectral_select import Visualizer, WavelengthResult, WavelengthBand, AnalysisMetrics; bands = [WavelengthBand(rank=i+1, excitation_nm=365.0, emission_nm=400.0+i*10, emission_band_index=i, influence_score=1.0-i*0.05) for i in range(10)]; result = WavelengthResult(sample_name='test', selected_bands=bands, metrics=AnalysisMetrics.from_bands(bands, 100)); v = Visualizer.from_result(result); paths = v.plot_all(); print(f'Generated {len(paths)} plots')"</verify>
  <done>plot_all orchestrates all visualizations, Visualizer is complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] plot_confusion_matrix generates valid confusion matrix PNG
- [ ] plot_per_class_metrics creates two-panel bar chart
- [ ] plot_accuracy_heatmap shows spatial correct/incorrect map
- [ ] plot_roi_overlay creates 3-panel ROI visualization
- [ ] plot_all returns list of generated paths
- [ ] No NotImplementedError stubs remain
- [ ] All methods have docstrings
</verification>

<success_criteria>
- All clustering/validation visualization methods implemented
- Methods work with numpy arrays for ground truth and predictions
- ROI overlay correctly maps cluster colors to ground truth classes
- plot_all generates all available visualizations
- Visualizer class is feature-complete for Phase 5 scope
- Phase 5 complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-visualization-module/05-03-SUMMARY.md`
</output>
