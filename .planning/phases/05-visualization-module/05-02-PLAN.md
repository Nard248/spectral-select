---
phase: 05-visualization-module
plan: 02
type: execute
---

<objective>
Implement wavelength heatmaps, spectral plots, and the summary dashboard.

Purpose: Provide publication-quality visualizations of wavelength selection results for analysis and paper figures.
Output: Working plot_influence_heatmap, plot_wavelength_scatter, plot_excitation_distribution, plot_influence_ranking, plot_wavelength_coverage, and plot_summary_dashboard methods.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visualization-module/05-01-SUMMARY.md

**Source files to reference:**
@spectral_select/visualizer.py
@spectral_select/types.py
@wavelength_analysis/core/visualization.py

**Original visualization patterns:**
The WavelengthVisualizer in wavelength_analysis/core/visualization.py has these methods:
- create_influence_heatmap(): Heatmap of influence scores across excitations
- create_wavelength_scatter(): Scatter plot of top wavelength combinations
- create_excitation_distribution(): Bar chart of selections per excitation
- create_influence_ranking_plot(): Line plot of influence vs rank
- create_wavelength_coverage_plot(): 2D coverage showing selected vs available
- create_summary_dashboard(): 6-panel combined figure

**Key data from WavelengthResult:**
- selected_bands: List[WavelengthBand] with rank, excitation_nm, emission_nm, emission_band_index, influence_score
- metrics: AnalysisMetrics with total_bands_available, bands_selected, compression_ratio, score stats
- excitation_wavelengths: unique sorted list from selected bands
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement plot_influence_heatmap and plot_influence_ranking</name>
  <files>spectral_select/visualizer.py</files>
  <action>
Replace NotImplementedError stubs with implementations:

1. plot_influence_heatmap(self, influence_matrix: Optional[Dict[float, np.ndarray]] = None) -> Path:
   - If influence_matrix not provided and has_result, build from _result.selected_bands
   - Build matrix: rows = excitations, cols = emission band indices
   - For each band in result.selected_bands: matrix[ex_idx, band.emission_band_index] = band.influence_score
   - Use log scale for visualization (np.log10(matrix + 1e-10))
   - Create figure with imshow, colorbar labeled "Log10(Influence Score)"
   - Y-axis: excitation wavelengths, X-axis: emission band index
   - Title: "Wavelength Influence Heatmap (Log Scale)"
   - Save via _save_figure, return path

2. plot_influence_ranking(self) -> Path:
   - Requires has_result (raise ValueError if not)
   - Extract ranks and influences from _result.selected_bands
   - Line plot with markers: x=rank, y=influence_score
   - Auto-scale y-axis to log if max/min ratio > 100
   - Title: "Influence Score vs. Band Ranking"
   - Grid, labels, save via _save_figure

Both methods follow original WavelengthVisualizer patterns but use WavelengthResult data.
  </action>
  <verify>python -c "from spectral_select import Visualizer, WavelengthResult, WavelengthBand, AnalysisMetrics; bands = [WavelengthBand(rank=i+1, excitation_nm=365.0, emission_nm=400.0+i*10, emission_band_index=i, influence_score=1.0-i*0.05) for i in range(10)]; result = WavelengthResult(sample_name='test', selected_bands=bands, metrics=AnalysisMetrics.from_bands(bands, 100)); v = Visualizer.from_result(result); print(v.plot_influence_ranking())"</verify>
  <done>Influence heatmap and ranking plots generate and save correctly</done>
</task>

<task type="auto">
  <name>Task 2: Implement plot_wavelength_scatter and plot_excitation_distribution</name>
  <files>spectral_select/visualizer.py</files>
  <action>
1. plot_wavelength_scatter(self, top_n: int = 50) -> Path:
   - Requires has_result
   - Extract top_n bands from _result.selected_bands
   - Scatter plot: x=excitation_nm, y=emission_nm
   - Color by influence_score (cmap='plasma')
   - Size by rank (larger for lower rank = more important)
   - Annotate top 20 with rank numbers
   - Colorbar: "Influence Score"
   - Title: "Top Wavelength Combinations\n(Size ∝ Ranking, Color ∝ Influence)"

2. plot_excitation_distribution(self) -> Path:
   - Requires has_result
   - Count selections per excitation: {ex_nm: count}
   - Bar chart with sorted excitation wavelengths as x-axis
   - Value labels on top of bars
   - Title: "Distribution of Selected Bands Across Excitation Wavelengths"
   - Y-axis: "Number of Selected Bands"
  </action>
  <verify>python -c "from spectral_select import Visualizer, WavelengthResult, WavelengthBand, AnalysisMetrics; bands = [WavelengthBand(rank=i+1, excitation_nm=365.0 if i<5 else 405.0, emission_nm=400.0+i*10, emission_band_index=i, influence_score=1.0-i*0.05) for i in range(10)]; result = WavelengthResult(sample_name='test', selected_bands=bands, metrics=AnalysisMetrics.from_bands(bands, 100)); v = Visualizer.from_result(result); print(v.plot_excitation_distribution())"</verify>
  <done>Scatter and distribution plots work correctly with multi-excitation data</done>
</task>

<task type="auto">
  <name>Task 3: Implement plot_wavelength_coverage and plot_summary_dashboard</name>
  <files>spectral_select/visualizer.py</files>
  <action>
1. plot_wavelength_coverage(self, total_bands_per_excitation: Optional[Dict[float, int]] = None) -> Path:
   - Shows selected vs available wavelength space
   - If total_bands provided: plot all available as light gray
   - Plot selected bands colored by influence
   - x=excitation_nm, y=emission_band_index
   - Title: "Wavelength Space Coverage\n(Gray: Available, Colored: Selected)"

2. plot_summary_dashboard(self) -> Path:
   - 2x3 grid of subplots (figsize=(20, 12))
   - Panel 1: Top 10 influence scores (bar chart)
   - Panel 2: Excitation distribution (pie chart with percentages)
   - Panel 3: Influence vs Rank (log scale line)
   - Panel 4: Top 30 wavelength pairs (scatter, simplified)
   - Panel 5: Statistics summary (text box with key metrics from AnalysisMetrics)
   - Panel 6: Mini heatmap of top excitations
   - Title: sample_name from result
   - Save as "summary_dashboard.png"

The dashboard provides a complete overview in one figure for papers/reports.
  </action>
  <verify>python -c "from spectral_select import Visualizer, WavelengthResult, WavelengthBand, AnalysisMetrics; bands = [WavelengthBand(rank=i+1, excitation_nm=365.0 if i<5 else 405.0, emission_nm=400.0+i*10, emission_band_index=i, influence_score=1.0-i*0.03) for i in range(20)]; result = WavelengthResult(sample_name='Test', selected_bands=bands, metrics=AnalysisMetrics.from_bands(bands, 500)); v = Visualizer.from_result(result); print(v.plot_summary_dashboard())"</verify>
  <done>Coverage plot and summary dashboard generate complete visualizations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] plot_influence_heatmap generates valid heatmap PNG
- [ ] plot_influence_ranking handles log scale correctly
- [ ] plot_wavelength_scatter shows colored scatter with annotations
- [ ] plot_excitation_distribution creates bar chart with correct counts
- [ ] plot_wavelength_coverage shows selected vs available space
- [ ] plot_summary_dashboard creates 6-panel figure
- [ ] All plots save to output_dir with correct dpi
</verification>

<success_criteria>
- All 6 wavelength visualization methods implemented
- Methods work with WavelengthResult data
- Plots match quality of original wavelength_analysis/core/visualization.py
- Consistent styling across all plots
- No matplotlib warnings
</success_criteria>

<output>
After completion, create `.planning/phases/05-visualization-module/05-02-SUMMARY.md`
</output>
