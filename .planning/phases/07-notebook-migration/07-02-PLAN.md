---
phase: 07-notebook-migration
plan: 02
type: execute
---

<objective>
Update all 4 analysis notebooks to use spectral_select.compat imports.

Purpose: Replace scattered sys.path manipulation and scripts.* imports with clean spectral_select.compat imports.
Output: All 4 notebooks use spectral_select.compat imports instead of scripts.* imports.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-notebook-migration/07-01-PLAN.md

**Notebooks to update:**
1. notebooks/complete_lime_analysis_notebook.ipynb
2. notebooks/complete_kiwi_analysis_notebook.ipynb
3. notebooks/complete_lichens_analysis_notebook.ipynb
4. notebooks/complete_lichens_2_analysis_notebook.ipynb

**Import pattern to replace:**

OLD (remove sys.path manipulation, replace scripts.* imports):
```python
notebook_dir = Path().absolute()
root_dir = notebook_dir.parent
if str(root_dir) not in sys.path:
    sys.path.append(str(root_dir))

from scripts.data_processing.hyperspectral_processor import HyperspectralProcessor
from scripts.models.dataset import MaskedHyperspectralDataset, ...
```

NEW:
```python
from spectral_select.compat import (
    HyperspectralProcessor,
    MaskedHyperspectralDataset, load_hyperspectral_data, load_mask,
    HyperspectralCAEWithMasking,
    train_with_masking, evaluate_model_with_masking,
    create_rgb_visualization, visualize_reconstruction_comparison,
    overlay_clusters_on_rgb, visualize_4d_cluster_profiles_consistent,
    overlay_clusters_with_consistent_colors,
    run_pixel_wise_clustering, visualize_cluster_profiles,
    run_4d_pixel_wise_clustering_setup_color, create_vibrant_colors,
    run_4d_pixel_wise_clustering, visualize_4d_cluster_profiles,
)
```

Also update scripts.utils imports in later cells.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update complete_lime_analysis_notebook.ipynb imports</name>
  <files>notebooks/complete_lime_analysis_notebook.ipynb</files>
  <action>
1. In cell-1 (Setup cell): Replace the sys.path manipulation and all scripts.* imports with spectral_select.compat imports
2. Remove the sys.path lines but KEEP root_dir definition as it's used for result paths
3. In cell-3 (Part 1): Replace scripts.utils.process_trq_files import
4. In cell-6 (Part 2): Replace scripts.utils.masking_utils import
5. Scan for any other scripts.* imports and replace

Keep all other code unchanged - only modify import statements.
  </action>
  <verify>grep -c "from scripts\." notebooks/complete_lime_analysis_notebook.ipynb returns 0</verify>
  <done>No scripts.* imports remain, all replaced with spectral_select.compat imports</done>
</task>

<task type="auto">
  <name>Task 2: Update complete_kiwi_analysis_notebook.ipynb imports</name>
  <files>notebooks/complete_kiwi_analysis_notebook.ipynb</files>
  <action>
Same pattern as Task 1:
1. Replace Setup cell imports with spectral_select.compat imports
2. Keep root_dir definition for result paths
3. Replace scripts.utils imports in later cells
4. Verify no scripts.* imports remain
  </action>
  <verify>grep -c "from scripts\." notebooks/complete_kiwi_analysis_notebook.ipynb returns 0</verify>
  <done>No scripts.* imports remain in kiwi notebook</done>
</task>

<task type="auto">
  <name>Task 3: Update complete_lichens_analysis_notebook.ipynb imports</name>
  <files>notebooks/complete_lichens_analysis_notebook.ipynb</files>
  <action>
Same pattern as Task 1:
1. Replace Setup cell imports with spectral_select.compat imports
2. Keep root_dir definition for result paths
3. Replace scripts.utils imports in later cells
4. Verify no scripts.* imports remain
  </action>
  <verify>grep -c "from scripts\." notebooks/complete_lichens_analysis_notebook.ipynb returns 0</verify>
  <done>No scripts.* imports remain in lichens notebook</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `grep "from scripts\." notebooks/complete_lime_analysis_notebook.ipynb` returns empty
- [ ] `grep "from scripts\." notebooks/complete_kiwi_analysis_notebook.ipynb` returns empty
- [ ] `grep "from scripts\." notebooks/complete_lichens_analysis_notebook.ipynb` returns empty
- [ ] All notebooks still have valid Python syntax
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No scripts.* imports remain in any of the 3 notebooks
- Import cells use clean spectral_select.compat imports
</success_criteria>

<output>
After completion, create `.planning/phases/07-notebook-migration/07-02-SUMMARY.md`
</output>
