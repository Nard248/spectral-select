---
phase: 07-notebook-migration
plan: 03
type: execute
---

<objective>
Update lichens_2 notebook and verify all notebooks can import successfully.

Purpose: Complete the migration of the final notebook and verify the spectral_select.compat imports work correctly in all notebooks.
Output: All 4 notebooks migrated and import verification passed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-notebook-migration/07-01-PLAN.md
@.planning/phases/07-notebook-migration/07-02-PLAN.md

**Constraint from PROJECT.md:** "Notebook Compatibility: Existing Jupyter notebooks in notebooks/ must work after migration to new imports"

**Final notebook to update:**
- notebooks/complete_lichens_2_analysis_notebook.ipynb

**Verification approach:**
Use Python to verify all spectral_select.compat imports work correctly.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update complete_lichens_2_analysis_notebook.ipynb imports</name>
  <files>notebooks/complete_lichens_2_analysis_notebook.ipynb</files>
  <action>
Same pattern as 07-02 tasks:
1. Replace Setup cell imports with spectral_select.compat imports
2. Keep root_dir definition for result paths
3. Replace scripts.utils imports in later cells
4. Verify no scripts.* imports remain
  </action>
  <verify>grep -c "from scripts\." notebooks/complete_lichens_2_analysis_notebook.ipynb returns 0</verify>
  <done>No scripts.* imports remain in lichens_2 notebook</done>
</task>

<task type="auto">
  <name>Task 2: Verify imports work across all notebooks</name>
  <files>notebooks/*.ipynb</files>
  <action>
Verify that all spectral_select.compat imports work by running Python import statements.

Test that the complete import list from the notebooks works:
- All data processing imports
- All model imports
- All training imports
- All visualization imports
- All clustering imports
- All utility imports

Also confirm no scripts.* imports remain in any notebook using grep.
  </action>
  <verify>
python -c "from spectral_select.compat import HyperspectralProcessor, MaskedHyperspectralDataset, HyperspectralCAEWithMasking, train_with_masking, evaluate_model_with_masking, create_rgb_visualization, run_4d_pixel_wise_clustering"
grep "from scripts\." notebooks/*.ipynb returns empty
  </verify>
  <done>All notebooks' import statements work without ImportError</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `grep "from scripts\." notebooks/complete_lichens_2_analysis_notebook.ipynb` returns empty
- [ ] `grep "from scripts\." notebooks/*.ipynb` returns empty (all 4 notebooks)
- [ ] Import verification command succeeds without errors
- [ ] Phase 7 complete - all notebooks migrated to spectral_select.compat imports
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No scripts.* imports remain in any notebook
- All spectral_select.compat imports work correctly
- Phase 7: Notebook Migration is complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-notebook-migration/07-03-SUMMARY.md`
</output>
