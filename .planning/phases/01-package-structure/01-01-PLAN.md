---
phase: 01-package-structure
plan: 01
type: execute
---

<objective>
Create the spectral_select package skeleton with pyproject.toml for editable installation.

Purpose: Establish the foundational package structure that all subsequent phases will build upon. This creates the import path `spectral_select` and enables `pip install -e .` for development.

Output: A minimal but valid Python package that can be imported, with proper project metadata.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md

**Tech stack:** Python 3.11+, pip, virtual environment at .venv/
**Naming conventions:** snake_case for modules, PascalCase for classes
**Package name:** spectral_select (per PROJECT.md decision)
**Python requirement:** >= 3.11 (per PROJECT.md Out of Scope)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml with project metadata</name>
  <files>pyproject.toml</files>
  <action>
Create pyproject.toml at project root with:
- [build-system] using setuptools and wheel
- [project] section with:
  - name = "spectral-select" (PyPI naming convention uses hyphens)
  - version = "0.1.0"
  - description from PROJECT.md core value
  - requires-python = ">=3.11"
  - dependencies list from requirements.txt (numpy, scipy, pandas, torch, scikit-learn, matplotlib, seaborn, tifffile, Pillow, h5py, tqdm, kneed)
  - [project.optional-dependencies] with dev = ["pytest", "jupyterlab"]

Do NOT include tensorflow (less utilized per STACK.md), opencv-python (only in preprocessing scripts), or Java bridge deps (JPype, scyjava).

Use dynamic versioning is unnecessary - keep it simple with static version for now.
  </action>
  <verify>python -c "import tomllib; tomllib.load(open('pyproject.toml', 'rb'))" exits with code 0</verify>
  <done>Valid pyproject.toml exists at project root with correct metadata and dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create spectral_select package directory with __init__.py</name>
  <files>spectral_select/__init__.py</files>
  <action>
Create directory spectral_select/ at project root (not inside wavelength_analysis/).

Create spectral_select/__init__.py with:
- Module docstring explaining the package purpose
- __version__ = "0.1.0" (matching pyproject.toml)
- __all__ list that will export public API (empty for now, populated in 01-02)
- Placeholder comment indicating where future exports will go

Keep minimal - this is just the skeleton. Actual classes come in later phases.
  </action>
  <verify>python -c "import spectral_select; print(spectral_select.__version__)" outputs "0.1.0"</verify>
  <done>spectral_select/__init__.py exists and is importable with __version__ attribute</done>
</task>

<task type="auto">
  <name>Task 3: Verify editable installation works</name>
  <files>None (verification only)</files>
  <action>
Run pip install -e . in the project root to install package in development mode.

If installation fails, diagnose and fix pyproject.toml or package structure.

After installation, verify import works from a different directory to confirm package is properly installed.
  </action>
  <verify>pip show spectral-select shows package info; python -c "import spectral_select" from any directory succeeds</verify>
  <done>Package installs in editable mode and imports correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pyproject.toml is valid TOML and contains required sections
- [ ] spectral_select/ directory exists at project root
- [ ] spectral_select/__init__.py exists with __version__
- [ ] pip install -e . succeeds
- [ ] import spectral_select works from Python REPL
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Package is importable as `spectral_select`
- `pip show spectral-select` displays correct metadata
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-structure/01-01-SUMMARY.md`
</output>
