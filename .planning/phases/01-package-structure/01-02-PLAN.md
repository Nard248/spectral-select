---
phase: 01-package-structure
plan: 02
type: execute
---

<objective>
Create module structure and define public API exports for the spectral_select package.

Purpose: Establish the submodule layout that will house the refactored code and define the public interface users will import from.

Output: Complete package skeleton with submodules, typed stubs, and a clear public API ready for implementation in subsequent phases.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01-package-structure/01-01-PLAN.md

**Depends on:** Plan 01-01 completed (package skeleton exists)

**Target API from PROJECT.md:**
- `Analyzer` class - core wavelength selection engine
- `Config` class - unified configuration
- `Visualizer` class - plotting and visualization

**Source mapping from STRUCTURE.md:**
- wavelength_analysis/core/analyzer.py → spectral_select/analyzer.py
- wavelength_analysis/core/config.py → spectral_select/config.py
- wavelength_analysis/core/visualization.py → spectral_select/visualizer.py
- wavelength_analysis/ground_truth_validation.py → spectral_select/validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create submodule structure with __init__.py files</name>
  <files>spectral_select/config.py, spectral_select/analyzer.py, spectral_select/visualizer.py, spectral_select/validation.py, spectral_select/types.py</files>
  <action>
Create the following empty module files with docstrings only (implementation comes in later phases):

1. spectral_select/config.py
   - Docstring: "Configuration system for spectral analysis."
   - Add placeholder: `class Config: pass` with docstring noting "Unified configuration - see Phase 2"

2. spectral_select/analyzer.py
   - Docstring: "Core wavelength selection analysis engine."
   - Add placeholder: `class Analyzer: pass` with docstring noting "Main analyzer - see Phase 4"

3. spectral_select/visualizer.py
   - Docstring: "Visualization utilities for spectral analysis."
   - Add placeholder: `class Visualizer: pass` with docstring noting "Visualization - see Phase 5"

4. spectral_select/validation.py
   - Docstring: "Ground truth validation metrics."
   - Add placeholder: `class Validator: pass` with docstring noting "Validation - see Phase 6"

5. spectral_select/types.py
   - Docstring: "Data types and type aliases for spectral analysis."
   - Add placeholder imports: `from typing import TypeAlias` and define `SpectraArray: TypeAlias = "np.ndarray"` as example

Do NOT implement any logic - just structure. This creates the import paths without coupling to implementation.
  </action>
  <verify>python -c "from spectral_select import config, analyzer, visualizer, validation, types" succeeds</verify>
  <done>All submodules exist and are importable</done>
</task>

<task type="auto">
  <name>Task 2: Update __init__.py with public API exports</name>
  <files>spectral_select/__init__.py</files>
  <action>
Update spectral_select/__init__.py to export the public API:

1. Import the placeholder classes:
   - from .config import Config
   - from .analyzer import Analyzer
   - from .visualizer import Visualizer
   - from .validation import Validator

2. Update __all__ to list public exports:
   - __all__ = ["Config", "Analyzer", "Visualizer", "Validator", "__version__"]

3. Add module docstring with usage example showing the target API:
```python
"""
spectral_select - Wavelength selection for hyperspectral imaging.

Usage:
    from spectral_select import Analyzer, Config

    config = Config(sample_name="Lichens_2")
    analyzer = Analyzer(config)
    results = analyzer.fit(data)
"""
```

This defines the user-facing API. Users will `from spectral_select import Analyzer` not `from spectral_select.analyzer import Analyzer`.
  </action>
  <verify>python -c "from spectral_select import Analyzer, Config, Visualizer, Validator; print('API exports work')"</verify>
  <done>Public API is accessible via top-level imports</done>
</task>

<task type="auto">
  <name>Task 3: Add py.typed marker for type checking</name>
  <files>spectral_select/py.typed</files>
  <action>
Create empty file spectral_select/py.typed to indicate the package supports type checking (PEP 561).

This enables type checkers (mypy, pyright, IDE tools) to read type hints from the package.
  </action>
  <verify>test -f spectral_select/py.typed</verify>
  <done>py.typed marker exists</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All submodules importable: config, analyzer, visualizer, validation, types
- [ ] Top-level imports work: `from spectral_select import Analyzer, Config`
- [ ] py.typed marker exists
- [ ] No import errors when running `python -c "import spectral_select"`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Package structure matches target API from PROJECT.md
- Phase 1 complete - package ready for implementation phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-structure/01-02-SUMMARY.md`

This SUMMARY.md should include:
- Note that Phase 1 is complete
- List of created files
- Statement that package is ready for Phase 2 (Config System)
</output>
