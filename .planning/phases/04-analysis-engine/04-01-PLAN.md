---
phase: 04-analysis-engine
plan: 01
type: execute
domain: python
---

<objective>
Create Analyzer class skeleton with scikit-learn-style public API (fit/transform/get_wavelengths).

Purpose: Establish the main interface for wavelength selection that users will import and use.
Output: Working Analyzer class that can be instantiated with Config, ready for method implementations.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summaries (relevant to this phase):
@.planning/phases/02-config-system/02-01-SUMMARY.md
@.planning/phases/03-core-data-types/03-02-SUMMARY.md

# Key files to understand:
@spectral_select/config.py
@spectral_select/types.py
@spectral_select/protocols.py

# Original implementation (reference only - extract patterns, don't copy verbatim):
@wavelength_analysis/core/analyzer.py

**Tech stack available:** dataclasses, typing, pathlib, torch
**Established patterns:** Protocol-based interfaces, Config dataclass, WavelengthResult output type
**Constraining decisions:**
- Phase 02-01: Use TYPE_CHECKING guard for protocol references
- Phase 03-02: Results use WavelengthBand/WavelengthResult for output
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Analyzer class with __init__ and device setup</name>
  <files>spectral_select/analyzer.py</files>
  <action>
Replace placeholder Analyzer class with real implementation:

1. Import typing, torch, pathlib, logging, and local modules (Config, SpectraData, WavelengthResult)
2. Create module logger: `logger = logging.getLogger(__name__)`
3. Implement `__init__(self, config: Config)`:
   - Store config reference
   - Setup device: `torch.device(config.device if torch.cuda.is_available() else 'cpu')`
   - Initialize state attributes as None: `_data`, `_model`, `_baseline_latent`, `_influence_matrix`, `_result`
   - Create output directory if config.output_dir is set
   - Log initialization: `logger.info(f"Analyzer initialized for {config.sample_name}")`

4. Add properties for state access:
   - `is_fitted -> bool`: Return True if `_result` is not None
   - `result -> Optional[WavelengthResult]`: Return `_result`
   - `influence_matrix -> Optional[Dict]`: Return `_influence_matrix`

5. Add `__repr__` for debugging.

Use logging instead of print statements. Reference original implementation for logic but use clean code style.
  </action>
  <verify>python -c "from spectral_select import Analyzer, Config; a = Analyzer(Config()); print(a)"</verify>
  <done>Analyzer class instantiates with Config, device setup works, properties accessible</done>
</task>

<task type="auto">
  <name>Task 2: Add public API method stubs with docstrings</name>
  <files>spectral_select/analyzer.py</files>
  <action>
Add method stubs for the scikit-learn-style public API:

1. `fit(self, data: SpectraData) -> "Analyzer"`:
   - Docstring explaining it runs full analysis pipeline
   - For now: raise NotImplementedError with message "Implemented in 04-02/04-03"
   - Return type hint: Self (for method chaining)

2. `transform(self, data: SpectraData) -> SpectraData`:
   - Docstring: Apply fitted selection to extract only selected wavelengths
   - Raise NotImplementedError (requires fit to be done first)

3. `fit_transform(self, data: SpectraData) -> SpectraData`:
   - Docstring: Convenience method combining fit and transform
   - Implementation: `return self.fit(data).transform(data)`

4. `get_wavelengths(self) -> List[WavelengthBand]`:
   - Docstring: Return list of selected wavelength combinations
   - Check if fitted, raise RuntimeError if not
   - Return `self._result.selected_bands` if fitted

5. `save_results(self, output_dir: Optional[Path] = None) -> Path`:
   - Docstring: Save analysis results to JSON and optional TIFF layers
   - Raise NotImplementedError (implemented in 04-04)

6. Add type hints for all methods using `from __future__ import annotations`.
  </action>
  <verify>python -c "from spectral_select import Analyzer, Config; a = Analyzer(Config()); help(a.fit); help(a.get_wavelengths)"</verify>
  <done>All public methods have stubs with proper docstrings and type hints</done>
</task>

<task type="auto">
  <name>Task 3: Update package exports and add type stubs</name>
  <files>spectral_select/__init__.py</files>
  <action>
1. Ensure Analyzer is exported at package root (should already be there from Phase 1)
2. Verify imports work: `from spectral_select import Analyzer, Config, SpectraData, WavelengthResult`
3. Add `__all__` list if not present, including: Analyzer, Config, SpectraData, WavelengthResult, WavelengthBand, AnalysisMetrics

No changes to py.typed needed (already created in Phase 1).
  </action>
  <verify>python -c "from spectral_select import Analyzer, Config, SpectraData, WavelengthResult, WavelengthBand; print('All exports OK')"</verify>
  <done>Package exports complete, type stubs work</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from spectral_select import Analyzer, Config; Analyzer(Config())"` works
- [ ] `python -m py_compile spectral_select/analyzer.py` passes
- [ ] All public methods have docstrings
- [ ] No print statements in new code (only logging)
</verification>

<success_criteria>

- Analyzer class exists with __init__, fit, transform, fit_transform, get_wavelengths, save_results
- Instantiation with Config works
- Device detection (cuda/cpu) works
- Methods have proper docstrings and type hints
- Package exports updated
</success_criteria>

<output>
After completion, create `.planning/phases/04-analysis-engine/04-01-SUMMARY.md`
</output>
