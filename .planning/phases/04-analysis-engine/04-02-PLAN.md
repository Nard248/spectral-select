---
phase: 04-analysis-engine
plan: 02
type: execute
domain: python
---

<objective>
Implement data loading and autoencoder integration in the Analyzer class.

Purpose: Enable Analyzer to load SpectraData and interface with the HyperspectralCAEWithMasking model.
Output: Analyzer can load data, create/load autoencoder model, and setup baseline latent representations.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analysis-engine/04-01-SUMMARY.md

# Key files:
@spectral_select/analyzer.py
@spectral_select/types.py
@scripts/models/__init__.py
@scripts/models/autoencoder.py
@scripts/models/dataset.py

# Original implementation (reference for algorithm logic):
@wavelength_analysis/core/analyzer.py (lines 64-153)

**Constraining decisions:**
- Phase 03-01: SpectraData provides excitation_wavelengths, spatial_shape, mask
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add internal data loading method</name>
  <files>spectral_select/analyzer.py</files>
  <action>
Add private method _load_data(self, data: SpectraData) -> None:

1. Store SpectraData reference: self._data = data
2. Log data info: excitation wavelengths, spatial shape, mask presence
3. Convert SpectraData to format expected by MaskedHyperspectralDataset:
   - Build data_dict structure with cube and wavelengths per excitation
   - Extract mask from SpectraData

4. Import MaskedHyperspectralDataset from scripts.models

5. Create dataset: self._dataset = MaskedHyperspectralDataset(data_dict, mask, normalize=True)

6. Store spatial dimensions and excitation info for later use.

Note: The sys.path manipulation from original code is temporary technical debt. Add TODO comment for Phase 8 cleanup.
  </action>
  <verify>python -c "from spectral_select.analyzer import Analyzer; import inspect; print('_load_data' in dir(Analyzer))"</verify>
  <done>_load_data method exists and converts SpectraData to dataset format</done>
</task>

<task type="auto">
  <name>Task 2: Add model loading and training integration</name>
  <files>spectral_select/analyzer.py</files>
  <action>
Add private method _load_or_train_model(self) -> None:

1. Import HyperspectralCAEWithMasking from scripts.models
2. Get data from dataset using get_all_data()
3. Initialize model with correct architecture parameters (k1=20, k3=20, filter_size=5)

4. Attempt to load pretrained weights from config.model_path:
   - If file exists: load state dict using torch.load
   - If file missing or size mismatch: call _train_new_model()
   - Handle RuntimeError for architecture mismatch

5. Move model to device
6. Set model to evaluation mode

Add private method _train_new_model(self) -> None:
1. Log warning that training new model
2. Import train_with_masking from scripts.models.training
3. Train with reasonable defaults (epochs=3000, lr=0.001)
4. Save to config.model_path if specified
5. Log completion

Use logging instead of print. Add progress indication via tqdm if available.
  </action>
  <verify>python -c "from spectral_select.analyzer import Analyzer; import inspect; print('_load_or_train_model' in dir(Analyzer))"</verify>
  <done>Model loading with fallback to training works</done>
</task>

<task type="auto">
  <name>Task 3: Add baseline setup method</name>
  <files>spectral_select/analyzer.py</files>
  <action>
Add private method _setup_baseline(self) -> None:

1. Get all data and spatial dimensions from dataset
2. Extract patches using config parameters:
   - patch_size, patch_stride, n_baseline_patches
   - Filter patches by mask validity (more than 50 percent valid pixels)

3. Store patch coordinates for reproducibility
4. Extract patches for each excitation wavelength
5. Encode patches through model to get baseline latent:
   - Move patches to device
   - Run model.encode() in no_grad context
   - Run model.decode() for baseline reconstruction

6. Store results in self._baseline_latent and self._baseline_reconstruction
7. Log baseline latent shape

Reference lines 108-153 of wavelength_analysis/core/analyzer.py for algorithm details.
  </action>
  <verify>python -c "from spectral_select.analyzer import Analyzer; import inspect; src = inspect.getsource(Analyzer._setup_baseline); print('baseline_latent' in src)"</verify>
  <done>Baseline setup creates latent representations for perturbation analysis</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python -m py_compile spectral_select/analyzer.py passes
- [ ] _load_data, _load_or_train_model, _train_new_model, _setup_baseline all exist
- [ ] No syntax errors in torch/numpy usage
- [ ] Logging used instead of print statements
</verification>

<success_criteria>

- Data loading converts SpectraData to MaskedHyperspectralDataset
- Model loading handles missing/mismatched model files gracefully
- Baseline setup creates latent representations
- All methods use logging
</success_criteria>

<output>
After completion, create .planning/phases/04-analysis-engine/04-02-SUMMARY.md
</output>
