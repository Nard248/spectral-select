---
phase: 04-analysis-engine
plan: 03
type: execute
domain: python
---

<objective>
Implement perturbation-based wavelength attribution in the Analyzer class.

Purpose: Core algorithm that identifies important wavelengths by perturbing latent dimensions.
Output: Analyzer can compute influence matrix mapping latent perturbations to emission band sensitivity.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analysis-engine/04-02-SUMMARY.md

# Key files:
@spectral_select/analyzer.py
@spectral_select/config.py

# Original implementation (reference for algorithm logic):
@wavelength_analysis/core/analyzer.py (lines 155-343)

**Tech stack:** torch, numpy, sklearn.preprocessing.StandardScaler, sklearn.decomposition.PCA
**Constraining decisions:**
- Config provides: dimension_selection_method, perturbation_method, perturbation_magnitudes, perturbation_directions, normalization_method
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement dimension selection methods</name>
  <files>spectral_select/analyzer.py</files>
  <action>
Add private method `_select_important_dimensions(self) -> None`:

1. Get latent tensor shape: (batch, channels, n_latent, h, w)
2. Flatten spatial dimensions for analysis
3. Implement three selection methods based on config.dimension_selection_method:

   **variance method:**
   - Compute variance across batch dimension
   - Higher variance = more informative dimension

   **activation method:**
   - Compute mean absolute activation
   - Higher activation = more used by model

   **pca method:**
   - Use StandardScaler and PCA from sklearn
   - Select dimensions with highest PCA loading contributions

4. Convert flat indices to (channel, latent, h, w) coordinates
5. Sort by importance score, keep top config.n_important_dimensions
6. Store as `self._important_dims: List[Tuple[float, Tuple[int,int,int,int]]]`
7. Log top 5 dimensions

Reference lines 155-204 of original analyzer.py for exact algorithm.
  </action>
  <verify>python -c "from spectral_select.analyzer import Analyzer; import inspect; src = inspect.getsource(Analyzer._select_important_dimensions); print('variance' in src and 'pca' in src)"</verify>
  <done>Three dimension selection methods implemented (variance, activation, pca)</done>
</task>

<task type="auto">
  <name>Task 2: Implement perturbation and influence measurement</name>
  <files>spectral_select/analyzer.py</files>
  <action>
Add private method `_compute_influence_scores(self) -> None`:

1. Initialize influence matrix: `{ex: np.zeros(n_bands) for ex in excitations}`

2. Add helper `_calculate_latent_statistics(latent_flat)`:
   - Compute mean, std, min, max, percentiles (5, 10, 25, 50, 75, 90, 95)
   - Return dict of torch tensors

3. Add helper `_calculate_perturbation_amount(coords, magnitude, sign, stats)`:
   - Implement three methods based on config.perturbation_method:
     - **percentile:** Shift toward target percentile
     - **standard_deviation:** Scale by std deviation
     - **absolute_range:** Scale by value range

4. Add helper `_measure_band_influence(perturbed_latent, importance_weight)`:
   - Decode perturbed latent
   - Compare to baseline reconstruction
   - Return mean absolute difference per emission band

5. Main loop:
   - For each important dimension:
     - For each magnitude in config.perturbation_magnitudes:
       - For each direction in config.perturbation_directions:
         - Apply perturbation
         - Measure influence
         - Accumulate weighted influence

6. Store result in `self._influence_matrix`
7. Log total perturbations performed

Reference lines 206-266 of original analyzer.py for algorithm.
  </action>
  <verify>python -c "from spectral_select.analyzer import Analyzer; import inspect; print('_compute_influence_scores' in dir(Analyzer))"</verify>
  <done>Perturbation analysis computes influence matrix</done>
</task>

<task type="auto">
  <name>Task 3: Implement influence normalization</name>
  <files>spectral_select/analyzer.py</files>
  <action>
Add private method `_normalize_influences(self) -> None`:

1. Implement based on config.normalization_method:

   **variance method:**
   - Divide influence by per-band variance in original data
   - Handles bands with low natural variance

   **max_per_excitation method:**
   - Normalize each excitation's influences to [0, 1]
   - Max influence per excitation becomes 1.0

   **none method:**
   - Skip normalization (raw influence scores)

2. Handle division by zero with epsilon or explicit check
3. Update `self._influence_matrix` in-place
4. Log normalization method applied

Reference lines 326-342 of original analyzer.py.
  </action>
  <verify>python -c "from spectral_select.analyzer import Analyzer; import inspect; src = inspect.getsource(Analyzer._normalize_influences); print('variance' in src and 'max_per_excitation' in src)"</verify>
  <done>Normalization methods applied to influence matrix</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile spectral_select/analyzer.py` passes
- [ ] _select_important_dimensions, _compute_influence_scores, _normalize_influences exist
- [ ] All three dimension selection methods work
- [ ] All three normalization methods work
- [ ] Influence matrix has correct structure (dict of numpy arrays)
</verification>

<success_criteria>

- Dimension selection supports variance, activation, pca methods
- Perturbation analysis computes influence per emission band
- Normalization supports variance, max_per_excitation, none methods
- Influence matrix ready for band selection in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/04-analysis-engine/04-03-SUMMARY.md`
</output>
