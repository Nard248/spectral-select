---
phase: 02-config-system
plan: 01
type: execute
---

<objective>
Create the unified Config dataclass with typed fields capturing all variant behaviors from the existing codebase.

Purpose: Establish the central configuration system that controls the entire pipeline, with pluggable component interfaces for extensibility.
Output: Working Config dataclass in `spectral_select/config.py` with validation and protocol definitions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-config-system/02-CONTEXT.md
@.planning/phases/01-package-structure/01-02-SUMMARY.md

# Existing config to migrate from:
@wavelength_analysis/core/config.py

# Target file (current placeholder):
@spectral_select/config.py

**Tech stack available:** Python 3.11, dataclasses, typing
**Established patterns:** Dataclass with __post_init__, type hints throughout
**Constraining decisions:**
- Phase 01: Export classes at package root for clean imports
- 02-CONTEXT: Fixed interfaces, flexible implementations (pluggable components)
- 02-CONTEXT: Dual registration - string identifiers for built-ins, direct class/function for custom
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Config dataclass with comprehensive typed fields</name>
  <files>spectral_select/config.py</files>
  <action>
Replace the placeholder Config class with a full dataclass implementation.

Include ALL configuration categories from existing AnalysisConfig plus new pluggable component fields:

**Data configuration:**
- sample_name: str
- data_path: Optional[Path]
- mask_path: Optional[Path]
- model_path: Optional[Path]
- output_dir: Optional[Path]

**Analysis parameters:**
- dimension_selection_method: str (default "activation")
- n_important_dimensions: int (default 15)
- perturbation_method: str (default "percentile")
- perturbation_magnitudes: List[float] (default [10, 20, 30])
- perturbation_directions: List[str] (default ["bidirectional"])
- normalization_method: str (default "variance")

**Selection parameters:**
- n_bands_to_select: int (default 30)
- n_layers_to_extract: int (default 10)

**Diversity constraint parameters:**
- use_diversity_constraint: bool (default False)
- diversity_method: str (default "mmr")
- lambda_diversity: float (default 0.5)
- min_distance_nm: float (default 15.0)

**Output configuration:**
- save_tiff_layers: bool (default True)
- save_visualizations: bool (default True)
- save_detailed_results: bool (default True)

**Technical parameters:**
- device: str (default "cuda")
- n_baseline_patches: int (default 50)
- patch_size: int (default 32)
- patch_stride: int (default 16)
- random_seed: int (default 42)

**Pluggable components (NEW - from 02-CONTEXT vision):**
- classifier: Union[str, Type[ClassifierProtocol]] (default "knn")
- clustering: Union[str, Type[ClusteringProtocol]] (default "kmeans")
- autoencoder_architecture: Union[str, Type[AutoencoderProtocol]] (default "standard")
- wavelength_ranker: Union[str, Type[WavelengthRankerProtocol]] (default "perturbation")

Use `from __future__ import annotations` for forward references. Use `field(default_factory=...)` for mutable defaults. Use Path from pathlib for path fields (with Optional).

IMPORTANT: Do NOT import protocols that don't exist yet - use string type annotations with TYPE_CHECKING guard.
  </action>
  <verify>python -c "from spectral_select import Config; c = Config(); print(c.sample_name, c.classifier)"</verify>
  <done>Config dataclass instantiates with sensible defaults, all fields accessible with type hints</done>
</task>

<task type="auto">
  <name>Task 2: Add validation logic and define component protocol interfaces</name>
  <files>spectral_select/config.py, spectral_select/protocols.py</files>
  <action>
**In spectral_select/config.py:**

Add `__post_init__` method with validation:
1. Validate dimension_selection_method in ["variance", "activation", "pca"]
2. Validate perturbation_method in ["percentile", "standard_deviation", "absolute_range"]
3. Validate normalization_method in ["variance", "max_per_excitation", "none"]
4. Validate diversity_method in ["mmr", "min_distance", "none"]
5. Validate numeric ranges (n_important_dimensions > 0, lambda_diversity between 0 and 1, etc.)
6. Validate device in ["cuda", "cpu", "mps"]
7. Raise ValueError with clear message on invalid configuration

Add `_resolve_component` helper method that:
- If string, looks up in BUILT_IN_* registry dicts
- If class/callable, validates it's callable and returns it
- Raises ValueError if unknown string or invalid type

Add registry constants at module level:
- BUILT_IN_CLASSIFIERS = {"knn": None}  # Placeholder - actual impl in Phase 4
- BUILT_IN_CLUSTERING = {"kmeans": None}
- BUILT_IN_AUTOENCODERS = {"standard": None}
- BUILT_IN_WAVELENGTH_RANKERS = {"perturbation": None}

**In spectral_select/protocols.py (NEW FILE):**

Create Protocol classes (from typing import Protocol, runtime_checkable):
- ClassifierProtocol: fit(X, y) -> self, predict(X) -> ndarray
- ClusteringProtocol: fit_predict(X) -> ndarray
- AutoencoderProtocol: encode(X) -> ndarray, decode(Z) -> ndarray
- WavelengthRankerProtocol: rank(data, model) -> List[Dict]

Use @runtime_checkable decorator for isinstance checks.

**Update spectral_select/__init__.py:**
Export Config and protocol classes.
  </action>
  <verify>python -c "from spectral_select import Config; Config(dimension_selection_method='invalid')" should raise ValueError</verify>
  <done>Config validates inputs in __post_init__, protocol classes defined and exported, invalid config raises clear ValueError</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from spectral_select import Config; c = Config(); print(c)"` works
- [ ] `python -c "from spectral_select import Config; Config(n_important_dimensions=-1)"` raises ValueError
- [ ] `python -c "from spectral_select.protocols import ClassifierProtocol"` works
- [ ] All type hints are correct (no mypy errors if run)
</verification>

<success_criteria>

- Config dataclass implemented with all fields from existing AnalysisConfig plus pluggable component fields
- Validation in __post_init__ catches invalid configurations
- Protocol interfaces defined for future implementation
- Clean imports work: `from spectral_select import Config`
</success_criteria>

<output>
After completion, create `.planning/phases/02-config-system/02-01-SUMMARY.md` following the summary template.
</output>
