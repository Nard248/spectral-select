---
phase: 10-results-organization
plan: 03
type: execute
---

<objective>
Add run metadata/provenance tracking and comprehensive tests for ResultsManager.

Purpose: Enable reproducibility by capturing environment info with each run, and ensure the results organization system is well-tested.
Output: Extended ResultsManager with metadata capture, new test file for results module.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Previous plans in this phase:
@.planning/phases/10-results-organization/10-01-SUMMARY.md
@.planning/phases/10-results-organization/10-02-SUMMARY.md

# Key source files:
@spectral_select/results.py
@tests/test_config.py

**Tech stack available:** pytest, dataclasses, subprocess (for git), platform, sys
**Established patterns:**
- Test fixtures with function scope for mutable data
- Test class naming: Test{ClassName}{Aspect}
- Both positive and negative test cases

**Constraining decisions:**
- 08-01: Fixed random seed (42) for reproducible test data
- 08-02: Extended tests beyond minimums for comprehensive coverage
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run metadata and provenance tracking</name>
  <files>spectral_select/results.py</files>
  <action>
Extend ResultsManager with metadata/provenance capture:

1. Add `save_run_metadata(config: Config, extra: Optional[Dict] = None)` method:
   - Saves to `run_dir/run_metadata.json`
   - Includes:
     - `run_id`: The run identifier
     - `timestamp`: ISO format datetime
     - `config_snapshot`: config.to_dict()
     - `environment`: dict with Python version, platform, key package versions
     - `git_info`: dict with commit hash, branch, dirty status (if in git repo)
     - `extra`: Any additional user-provided metadata

2. Add helper `_get_environment_info() -> Dict`:
   - Python version (sys.version)
   - Platform (platform.platform())
   - Package versions for: numpy, torch, spectral_select (if available)

3. Add helper `_get_git_info() -> Optional[Dict]`:
   - Use subprocess to run `git rev-parse HEAD` and `git rev-parse --abbrev-ref HEAD`
   - Check dirty status with `git status --porcelain`
   - Return None if not in git repo or git not available
   - Catch exceptions gracefully - git info is optional

4. Add `load_run_metadata() -> Dict` method:
   - Load and return run_metadata.json for current run
   - Raise FileNotFoundError if not exists

5. Add `list_run_files() -> List[Path]` method:
   - Return list of all files in current run_dir (recursive)
   - Useful for archiving/inspection
  </action>
  <verify>python -c "from spectral_select.results import ResultsManager; from pathlib import Path; rm = ResultsManager(Path('/tmp/meta_test'), 'sample'); print(rm._get_environment_info())"</verify>
  <done>ResultsManager can save run metadata with environment and git info</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive tests for ResultsManager</name>
  <files>tests/test_results.py</files>
  <action>
Create new test file `tests/test_results.py` with comprehensive coverage:

```python
"""Tests for ResultsManager output organization."""
import pytest
from pathlib import Path
import tempfile
import json
from spectral_select import ResultsManager, Config
```

Test classes to implement:

1. **TestResultsManagerInitialization** (5 tests):
   - test_creates_directory_structure
   - test_auto_generates_run_id
   - test_accepts_custom_run_id
   - test_validates_empty_sample_name
   - test_from_config_factory

2. **TestResultsManagerPaths** (4 tests):
   - test_get_model_path_best
   - test_get_model_path_final
   - test_get_result_path
   - test_get_layer_path

3. **TestResultsManagerOperations** (4 tests):
   - test_get_all_runs_empty
   - test_get_all_runs_multiple
   - test_from_existing_run_valid
   - test_from_existing_run_not_found

4. **TestResultsManagerMetadata** (4 tests):
   - test_save_run_metadata
   - test_load_run_metadata
   - test_environment_info_contains_python_version
   - test_list_run_files

Use `tempfile.TemporaryDirectory()` for all tests to ensure cleanup.
Use function-scoped fixtures for test directories.
Target: 17+ tests (4 tests beyond minimum requirements).
  </action>
  <verify>pytest tests/test_results.py -v --tb=short</verify>
  <done>All ResultsManager tests pass, coverage includes initialization, paths, operations, and metadata</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP with Phase 10 completion</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update ROADMAP.md to reflect Phase 10 planning completion:

1. Under "### Phase 10: Results Organization", update Plans section:
   ```
   Plans:
   - [ ] 10-01: ResultsManager class and checkpoint naming
   - [ ] 10-02: Analyzer and Visualizer integration
   - [ ] 10-03: Metadata tracking and tests
   ```

2. Update Progress table row for Phase 10:
   - Plans Complete: 0/3
   - Status: Planned

This is a documentation-only task to maintain roadmap accuracy.
  </action>
  <verify>grep -A5 "Phase 10:" .planning/ROADMAP.md</verify>
  <done>ROADMAP.md shows Phase 10 with 3 plans and correct status</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_results.py -v` passes all tests
- [ ] `pytest tests/ -v` - all existing tests still pass
- [ ] Run metadata includes git info when available
- [ ] Environment info captures Python version
- [ ] ROADMAP.md reflects Phase 10 plan structure
</verification>

<success_criteria>
- Run metadata captures config, environment, and git info
- 17+ tests for ResultsManager with comprehensive coverage
- All tests pass
- ROADMAP updated with Phase 10 plans
- Phase 10 complete with full results organization system
</success_criteria>

<output>
After completion, create `.planning/phases/10-results-organization/10-03-SUMMARY.md`

Include in summary:
- Total test count for results module
- Coverage of metadata features
- Note that Phase 10 is complete
</output>
