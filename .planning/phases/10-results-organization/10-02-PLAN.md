---
phase: 10-results-organization
plan: 02
type: execute
---

<objective>
Integrate ResultsManager into Analyzer and Visualizer for consistent output paths.

Purpose: Replace ad-hoc path construction in Analyzer.save_results() and Visualizer with centralized ResultsManager, ensuring all outputs go to the structured directory hierarchy.
Output: Modified analyzer.py and visualizer.py using ResultsManager for all output operations.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Previous plan in this phase:
@.planning/phases/10-results-organization/10-01-SUMMARY.md

# Key source files:
@spectral_select/analyzer.py
@spectral_select/visualizer.py
@spectral_select/results.py

**Tech stack available:** ResultsManager (from 10-01), pathlib, dataclasses
**Established patterns:**
- Factory methods (from_result, from_analyzer, from_config)
- Private attributes with public properties
- TYPE_CHECKING imports for circular import prevention

**Constraining decisions:**
- 10-01: ResultsManager creates structured directories with run_id
- Phase 4: Current save_results() saves JSON, TIFF, text to flat output_dir
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Analyzer to use ResultsManager</name>
  <files>spectral_select/analyzer.py</files>
  <action>
Modify Analyzer class to integrate ResultsManager:

1. Add optional `results_manager: Optional[ResultsManager]` parameter to __init__
   - If provided, use it for all output operations
   - If not provided, create one lazily when save_results() is called

2. Add `_results_manager` private attribute and `results_manager` property:
   - Property creates ResultsManager lazily from config if not set
   - Uses config.output_dir as base_dir, config.sample_name

3. Refactor `save_results()` method:
   - Use self.results_manager.get_result_path() for JSON output paths
   - Use self.results_manager.layers_dir for TIFF layers
   - Keep backward compatibility: if config.output_dir is set directly without ResultsManager, still works

4. Add `save_model()` method that delegates to ResultsManager:
   ```python
   def save_model(self, checkpoint_type: str = "final", metadata: Optional[Dict] = None) -> Path:
       """Save trained model to results directory."""
       return self.results_manager.save_model_checkpoint(
           self._model, checkpoint_type, metadata
       )
   ```

5. Update model training flow (if train_model exists) to use ResultsManager paths

Do NOT break existing API - all current code should continue to work with deprecation warnings if using old patterns.
  </action>
  <verify>python -c "from spectral_select import Analyzer, Config; a = Analyzer(Config(sample_name='test')); print(a.results_manager.run_dir)"</verify>
  <done>Analyzer uses ResultsManager for output paths, save_results() creates files in structured hierarchy</done>
</task>

<task type="auto">
  <name>Task 2: Update Visualizer to accept ResultsManager paths</name>
  <files>spectral_select/visualizer.py</files>
  <action>
Modify Visualizer to integrate with ResultsManager:

1. Add class method `from_results_manager(results_manager: ResultsManager, **kwargs)`:
   - Creates Visualizer with output_dir set to results_manager.viz_dir
   - Stores reference to results_manager for potential future use

2. Update `from_analyzer()` factory method:
   - If analyzer has results_manager, use results_manager.viz_dir as output_dir
   - Otherwise fall back to current behavior (visualizations/{sample_name})

3. Update `_save_figure()` to use consistent naming:
   - Current: `{name}.png`
   - Keep this simple pattern but ensure output_dir comes from ResultsManager when available

4. No changes needed to plotting methods - they already use self._output_dir

Maintain backward compatibility - direct instantiation with output_dir still works.
  </action>
  <verify>python -c "from spectral_select import Visualizer; from spectral_select.results import ResultsManager; from pathlib import Path; rm = ResultsManager(Path('/tmp/viz_test'), 'sample'); v = Visualizer.from_results_manager(rm); print(v.output_dir)"</verify>
  <done>Visualizer can be created from ResultsManager, output goes to viz_dir within run structure</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from spectral_select import Analyzer, Config"` succeeds
- [ ] Analyzer.results_manager property returns valid ResultsManager
- [ ] Visualizer.from_results_manager() creates visualizer with correct output_dir
- [ ] All existing tests pass: `pytest tests/ -v`
- [ ] No import errors or circular dependencies
</verification>

<success_criteria>
- Analyzer uses ResultsManager for all output operations
- Visualizer can be created from ResultsManager
- Backward compatibility maintained (existing code still works)
- All outputs go to structured directory when ResultsManager is used
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-results-organization/10-02-SUMMARY.md`
</output>
