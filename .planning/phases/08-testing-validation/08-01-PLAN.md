---
phase: 08-testing-validation
plan: 01
type: execute
---

<objective>
Set up pytest test infrastructure with synthetic data fixtures for the spectral_select library.

Purpose: Establish the foundation for all unit and integration tests with reusable fixtures that don't depend on large data files.
Output: tests/ directory with conftest.py and synthetic data generators for SpectraData, Config, and related types.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@spectral_select/__init__.py
@spectral_select/types.py
@spectral_select/config.py

**Tech stack available:** pytest (in pyproject.toml dev dependencies)
**Established patterns:** Dataclass-based types, factory methods, protocol-based interfaces
**Constraining decisions:**
- Phase 03-01: Exclude large arrays from SpectraData.to_dict() for lightweight serialization
- Phase 03-01: Allow empty excitations dict for SpectraData initialization
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/ directory structure and pytest configuration</name>
  <files>tests/__init__.py, tests/conftest.py, pytest.ini</files>
  <action>
Create tests/ directory at project root with:
1. Empty __init__.py to make it a package
2. pytest.ini at project root with:
   - testpaths = tests
   - python_files = test_*.py
   - python_functions = test_*
   - addopts = -v --tb=short
3. conftest.py with basic pytest configuration importing spectral_select

Do NOT add coverage configuration yet (that's in 08-04).
  </action>
  <verify>pytest --collect-only shows test collection works (0 tests initially)</verify>
  <done>tests/ directory exists, pytest.ini configured, conftest.py imports spectral_select without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create synthetic data fixtures in conftest.py</name>
  <files>tests/conftest.py</files>
  <action>
Add pytest fixtures to conftest.py for test data generation:

1. `sample_config` fixture - Returns a minimal Config with sample_name="test_sample"
2. `synthetic_spectra_data` fixture - Returns a SpectraData with:
   - Small synthetic cube (10x10x5 pixels, 3 excitations)
   - Random but reproducible data (use np.random.seed(42))
   - Valid mask (all True for simplicity)
   - Emission wavelengths matching cube dimensions
3. `synthetic_excitation_data` fixture - Returns ExcitationData for single excitation
4. `synthetic_wavelength_bands` fixture - Returns list of 5 WavelengthBand objects
5. `tmp_output_dir` fixture - Uses tmp_path to create temporary output directory

Use @pytest.fixture decorator with appropriate scope (function for mutable, session for immutable).
Import numpy as np and all types from spectral_select.
  </action>
  <verify>pytest --collect-only shows fixtures available; pytest tests/conftest.py -v passes</verify>
  <done>All 5 fixtures defined and importable, synthetic data has correct shapes and types</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest --collect-only` runs without errors
- [ ] `python -c "from tests.conftest import *"` works
- [ ] Fixtures generate valid spectral_select types
</verification>

<success_criteria>

- tests/ directory created with proper structure
- pytest.ini configures test discovery
- conftest.py provides 5+ reusable fixtures
- All fixtures generate valid spectral_select data types
- No import errors when running pytest
</success_criteria>

<output>
After completion, create `.planning/phases/08-testing-validation/08-01-SUMMARY.md`
</output>
