---
phase: 08-testing-validation
plan: 02
type: execute
---

<objective>
Create unit tests for Config class and core data types to verify correct behavior.

Purpose: Ensure Config validation, serialization, and data type construction work as designed.
Output: Unit test files for config.py and types.py with comprehensive coverage of public API.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-testing-validation/08-01-PLAN.md
@spectral_select/config.py
@spectral_select/types.py
@spectral_select/protocols.py

**Tech stack available:** pytest
**Established patterns:**
- Config uses @dataclass with validation in __post_init__
- YAML/JSON round-trip via from_yaml/to_yaml, from_json/to_json
- Data types use factory methods (from_bands, from_json)
**Constraining decisions:**
- Phase 02-02: Use yaml.safe_load/safe_dump for security
- Phase 02-02: Warn on unknown config keys (forward compatibility)
- Phase 03-02: Validate sequential ranks in WavelengthResult.__post_init__
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for Config class</name>
  <files>tests/test_config.py</files>
  <action>
Create tests/test_config.py with pytest tests for Config:

1. test_config_defaults - Verify default values are set correctly
2. test_config_custom_values - Verify custom values override defaults
3. test_config_validation_dimension_method - Invalid dimension_selection_method raises ValueError
4. test_config_validation_perturbation_method - Invalid perturbation_method raises ValueError
5. test_config_yaml_roundtrip - to_yaml then from_yaml produces equivalent Config (use tmp_path)
6. test_config_json_roundtrip - to_json then from_json produces equivalent Config (use tmp_path)
7. test_config_unknown_keys_warning - from_yaml with unknown keys emits warning (use pytest.warns)

Use fixtures from conftest.py where appropriate. Import Config from spectral_select.
  </action>
  <verify>pytest tests/test_config.py -v passes all tests</verify>
  <done>7+ tests for Config, all passing, covers validation and serialization</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for data types (SpectraData, WavelengthBand, WavelengthResult)</name>
  <files>tests/test_types.py</files>
  <action>
Create tests/test_types.py with pytest tests for core data types:

SpectraData tests:
1. test_spectra_data_creation - Basic instantiation with valid data
2. test_spectra_data_shape_validation - Mismatched cube/mask shapes raise ValueError
3. test_spectra_data_to_dict - Excludes large arrays, includes metadata

WavelengthBand tests:
4. test_wavelength_band_creation - Basic instantiation
5. test_wavelength_band_ordering - Comparison operators work correctly

WavelengthResult tests:
6. test_wavelength_result_creation - Basic instantiation with valid bands
7. test_wavelength_result_sequential_ranks - Non-sequential ranks raise ValueError
8. test_wavelength_result_from_bands - Factory method computes metrics correctly

ValidationMetrics tests:
9. test_validation_metrics_creation - Basic instantiation
10. test_validation_metrics_json_roundtrip - to_json/from_json preserves data

Use synthetic_wavelength_bands fixture where appropriate.
  </action>
  <verify>pytest tests/test_types.py -v passes all tests</verify>
  <done>10+ tests for data types, all passing, covers creation and validation</done>
</task>

<task type="auto">
  <name>Task 3: Protocol compliance tests</name>
  <files>tests/test_protocols.py</files>
  <action>
Create tests/test_protocols.py to verify protocol-based extensibility:

1. test_classifier_protocol_runtime_checkable - ClassifierProtocol is @runtime_checkable
2. test_clustering_protocol_runtime_checkable - ClusteringProtocol is @runtime_checkable
3. test_autoencoder_protocol_runtime_checkable - AutoencoderProtocol is @runtime_checkable
4. test_custom_classifier_implements_protocol - Custom class with fit/predict implements ClassifierProtocol

Create minimal mock classes that implement each protocol to verify isinstance() checks work.
This tests the extensibility mechanism documented in PROJECT.md.
  </action>
  <verify>pytest tests/test_protocols.py -v passes all tests</verify>
  <done>4+ protocol tests passing, verifies runtime_checkable works for extensibility</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_config.py -v` passes
- [ ] `pytest tests/test_types.py -v` passes
- [ ] `pytest tests/test_protocols.py -v` passes
- [ ] All tests use proper assertions (not boolean returns)
</verification>

<success_criteria>

- 20+ unit tests across 3 test files
- All tests passing
- Config validation and serialization fully tested
- Data type construction and validation tested
- Protocol extensibility mechanism verified
</success_criteria>

<output>
After completion, create `.planning/phases/08-testing-validation/08-02-SUMMARY.md`
</output>
