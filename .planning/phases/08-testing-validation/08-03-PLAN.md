---
phase: 08-testing-validation
plan: 03
type: execute
---

<objective>
Create integration tests verifying Analyzer and Validator produce correct outputs with synthetic data.

Purpose: Ensure end-to-end workflows function correctly and the refactored library behaves as expected.
Output: Integration test files that exercise full analysis and validation pipelines.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-testing-validation/08-01-PLAN.md
@.planning/phases/08-testing-validation/08-02-PLAN.md
@spectral_select/analyzer.py
@spectral_select/validation.py
@spectral_select/visualizer.py

**Tech stack available:** pytest, numpy, torch
**Established patterns:**
- Analyzer.fit() returns self for method chaining
- Validator.score() returns ARI as primary metric
- Visualizer.from_validation() factory for validation workflows
**Constraining decisions:**
- Phase 04-01: Device fallback chain: configured → availability check → cpu
- Phase 04-02: Train with 3000 epochs, lr=0.001 defaults
- Phase 06-01: Background pixels use -1 convention throughout
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyzer integration tests</name>
  <files>tests/test_analyzer_integration.py</files>
  <action>
Create tests/test_analyzer_integration.py with integration tests for Analyzer:

1. test_analyzer_initialization - Analyzer(config) creates valid instance
2. test_analyzer_fit_returns_self - fit(data) returns self for chaining
3. test_analyzer_fit_sets_fitted_flag - After fit(), analyzer.is_fitted is True
4. test_analyzer_get_wavelengths_before_fit - Raises error if called before fit()
5. test_analyzer_device_fallback - With invalid device, falls back to cpu

Note: Full end-to-end fit() requires trained autoencoder model which we don't have in fixtures.
Use pytest.mark.skip or mock the autoencoder for tests that need it.
Focus on testing the API contract, not the algorithm correctness (that would need real data).

Add a fixture `mock_analyzer` that creates an Analyzer with mocked internal components if needed.
  </action>
  <verify>pytest tests/test_analyzer_integration.py -v passes (some may be skipped)</verify>
  <done>5+ Analyzer tests, API contract verified, skipped tests documented</done>
</task>

<task type="auto">
  <name>Task 2: Validator integration tests</name>
  <files>tests/test_validator_integration.py</files>
  <action>
Create tests/test_validator_integration.py with integration tests for Validator:

1. test_validator_initialization - Validator(n_clusters) creates valid instance
2. test_validator_fit_with_ground_truth - fit(predictions, ground_truth) stores data
3. test_validator_score_returns_ari - score() returns float in [-1, 1] range
4. test_validator_generate_report - generate_report() returns markdown string
5. test_validator_to_json_roundtrip - to_json/from_json preserves metrics
6. test_validator_ground_truth_property - ground_truth property returns stored labels
7. test_load_ground_truth_from_png - Function loads PNG and extracts labels (use synthetic PNG)

Create a synthetic ground truth fixture:
- Small numpy array (10x10) with known class labels (0, 1, 2)
- Matching predictions array with some errors for realistic ARI

For test_load_ground_truth_from_png, create a minimal test PNG with known colors.
  </action>
  <verify>pytest tests/test_validator_integration.py -v passes all tests</verify>
  <done>7+ Validator tests, all passing, covers fit/score/report workflow</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_analyzer_integration.py -v` passes
- [ ] `pytest tests/test_validator_integration.py -v` passes
- [ ] Integration tests exercise real spectral_select code paths
- [ ] Skipped tests have clear skip reasons documented
</verification>

<success_criteria>

- 12+ integration tests across 2 test files
- Analyzer API contract fully tested
- Validator fit/score/report workflow tested
- Tests use synthetic data (no large file dependencies)
- All tests passing or intentionally skipped with documented reason
</success_criteria>

<output>
After completion, create `.planning/phases/08-testing-validation/08-03-SUMMARY.md`
</output>
