---
phase: 12-data-pipeline-improvements
plan: 03
type: execute
---

<objective>
Add comprehensive tests for the data pipeline (DataLoader, from_raw, to_pickle).

Purpose: Ensure data loading and serialization work correctly with proper test coverage.
Output: Test file with unit tests for data pipeline components.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plans in this phase:
@.planning/phases/12-data-pipeline-improvements/12-01-PLAN.md
@.planning/phases/12-data-pipeline-improvements/12-02-PLAN.md

# Testing patterns established:
@.planning/phases/08-testing-validation/08-01-SUMMARY.md
@.planning/phases/08-testing-validation/08-02-SUMMARY.md

# Existing test structure:
@tests/conftest.py
@tests/test_types.py

**Tech stack available:** pytest, numpy, tempfile
**Established patterns:**
- Function scope for test fixtures (mutable numpy data)
- Fixed random seed (42) for reproducible synthetic test data
- Test class naming: Test{ClassName}{Aspect}
- Both positive and negative test cases
**Constraining decisions:**
- Phase 08-01: Synthetic data: 10x10 spatial, 5 bands, 3 excitations
- Phase 08-02: Include both positive and negative test cases
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for DataLoader wrapper</name>
  <files>tests/test_loader.py</files>
  <action>
Create test file for DataLoader class testing.

Test classes needed:

TestDataLoaderInit:
- test_init_with_valid_path: Creates DataLoader with existing directory
- test_init_with_nonexistent_path: Raises error for missing directory
- test_init_default_parameters: Default cutoff_offset=30, verbose=True
- test_verbose_false_suppresses_output: No output when verbose=False

TestDataLoaderErrors:
- test_load_empty_directory: Raises DataLoadingError with helpful message
- test_load_no_im3_files: Raises DataLoadingError listing directory contents
- test_error_message_includes_path: Error contains the attempted path
- test_error_message_includes_hint: Error includes suggestion for fix

Note: Testing actual .im3 loading requires ImageJ and real data files, which may not be available in CI. Focus on error handling and initialization tests. Mark integration tests requiring real data as @pytest.mark.skip(reason="requires ImageJ and test data").

Do NOT:
- Create actual .im3 files in tests (binary format is complex)
- Make tests depend on ImageJ being installed
  </action>
  <verify>cd /Users/narekmeloyan/PycharmProjects/4D-Hyperspectral-Unsupervised-Clustering && python -m pytest tests/test_loader.py -v --collect-only 2>&1 | grep "test_"</verify>
  <done>At least 6 tests defined in tests/test_loader.py, all collectible by pytest</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for SpectraData.from_raw() and to_pickle()</name>
  <files>tests/test_types.py</files>
  <action>
Add tests to existing test_types.py for new SpectraData methods.

Add to TestSpectraData class or create new test classes:

TestSpectraDataFromRaw:
- test_from_raw_missing_path: Raises FileNotFoundError for missing directory
- test_from_raw_sample_name_from_directory: Derives sample_name from dir name
- test_from_raw_custom_sample_name: Uses provided sample_name

TestSpectraDataToPickle:
- test_to_pickle_creates_file: File exists after to_pickle
- test_to_pickle_creates_parent_dirs: Parent directories created automatically
- test_to_pickle_returns_path: Returns Path object
- test_to_pickle_roundtrip: from_pickle(to_pickle(data)) produces equivalent data

TestSpectraDataRoundTrip:
- test_roundtrip_preserves_excitations: Same excitation wavelengths
- test_roundtrip_preserves_cubes: Same cube data (np.allclose)
- test_roundtrip_preserves_mask: Same mask if provided
- test_roundtrip_preserves_metadata: Same metadata dict

Use synthetic SpectraData from existing conftest.py fixtures (synthetic_spectra_data) for round-trip tests. Note: The project uses pickle for numpy array serialization since JSON can't handle large arrays efficiently. from_pickle() already exists in the codebase.

Do NOT:
- Duplicate existing from_pickle tests
- Test ImageJ integration (handled in test_loader.py)
  </action>
  <verify>cd /Users/narekmeloyan/PycharmProjects/4D-Hyperspectral-Unsupervised-Clustering && python -m pytest tests/test_types.py -v --collect-only 2>&1 | grep -E "test_.*pickle|test_.*raw|test_.*roundtrip" | wc -l</verify>
  <done>At least 8 new tests for from_raw/to_pickle/roundtrip in test_types.py</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_loader.py -v` runs without import errors
- [ ] `python -m pytest tests/test_types.py -v` runs without import errors
- [ ] All new tests pass
- [ ] Round-trip tests verify data integrity
</verification>

<success_criteria>
- tests/test_loader.py created with 6+ tests
- tests/test_types.py extended with 8+ tests for new methods
- All tests pass
- Coverage includes error handling paths
- Phase 12 complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-pipeline-improvements/12-03-SUMMARY.md`
</output>
