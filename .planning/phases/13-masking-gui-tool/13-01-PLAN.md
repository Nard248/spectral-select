---
phase: 13-masking-gui-tool
plan: 01
type: execute
---

<objective>
Create the core ME-HSI Viewer application framework with data loading and basic display.

Purpose: Establish the main window structure, data loading capabilities, and foundational image display that all other features will build upon.
Output: spectral_select/viewer.py with ViewerApp class, data loading panel, and basic visualization.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference implementation (for data handling patterns):
@scripts/utils/masking_tool.py

# Integration targets:
@spectral_select/__init__.py
@spectral_select/types.py
@spectral_select/loader.py

# Prior phase context:
@.planning/phases/12-data-pipeline-improvements/12-01-SUMMARY.md

**Tech stack:** tkinter (stdlib), matplotlib (FigureCanvasTkAgg), numpy
**Established patterns:** SpectraData.from_pickle(), DataLoader for raw files
**Key data structure:** SpectraData.excitations dict maps wavelength (float) to ExcitationData with .cube (bands, height, width)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main window layout with professional structure</name>
  <files>spectral_select/viewer.py</files>
  <action>
Create spectral_select/viewer.py with ViewerApp class:

1. **Window structure** (1200x800 default, resizable):
   ```
   ┌─────────────────────────────────────────────────┐
   │ Toolbar: [Open] [Save Mask] │ Status bar right  │
   ├────────────┬────────────────────────────────────┤
   │            │                                    │
   │  Control   │     Main Canvas                    │
   │  Panel     │     (matplotlib figure)            │
   │  (250px)   │                                    │
   │            │                                    │
   │            ├────────────────────────────────────┤
   │            │  Info Bar (cursor pos, pixel val)  │
   └────────────┴────────────────────────────────────┘
   ```

2. **Control Panel** (ttk.LabelFrame sections, collapsible feel):
   - "Data" section: Open button, current file label
   - "Excitation" section: Combobox for wavelength selection (300, 310, 320...)
   - "Display" section: Placeholder for future controls (band slider, etc.)
   - "Tools" section: Placeholder for masking tools

3. **Main Canvas**:
   - matplotlib Figure embedded with FigureCanvasTkAgg
   - Navigation toolbar below canvas (zoom, pan, home, save)
   - Proper aspect ratio preservation

4. **Info Bar**:
   - Cursor position (X, Y)
   - Pixel value at cursor
   - Current excitation/band info

5. **Clean implementation**:
   - Type hints throughout
   - Logging instead of print()
   - No bare except blocks
   - Docstrings for public methods

Store state in instance variables:
- self.spectra_data: Optional[SpectraData]
- self.current_excitation: Optional[float]
- self.current_image: Optional[np.ndarray]
  </action>
  <verify>python -c "from spectral_select.viewer import ViewerApp; print('ViewerApp imports OK')"</verify>
  <done>ViewerApp class created with professional window layout, control panel, main canvas, and info bar</done>
</task>

<task type="auto">
  <name>Task 2: Implement data loading for SpectraData and raw files</name>
  <files>spectral_select/viewer.py</files>
  <action>
Add data loading functionality to ViewerApp:

1. **Open file dialog** (triggered by Open button):
   - File types: .pkl (SpectraData), .im3 (raw hyperspectral)
   - Remember last directory (tk.filedialog initialdir)

2. **Load SpectraData** (.pkl files):
   - Use SpectraData.from_pickle(path)
   - Populate excitation combobox with sorted wavelengths from spectra_data.excitations.keys()
   - Auto-select first excitation
   - Update window title with filename

3. **Load raw .im3 files** (stretch goal, graceful failure):
   - Try: Use DataLoader to load raw data
   - If ImageJ not available: Show info dialog explaining requirement
   - Convert loaded data to SpectraData format

4. **Display initial image**:
   - On load, create RGB visualization from first excitation's cube
   - Use mean projection across bands as grayscale, or RGB from 3 representative bands
   - Apply auto-contrast (2nd-98th percentile)

5. **Excitation selection handler**:
   - When combobox changes, update display with new excitation's data
   - Preserve zoom/pan state if possible

6. **Error handling**:
   - DataLoadingError: Show messagebox with helpful message
   - Invalid file format: Clear message about expected format
   - Update status bar with loading state
  </action>
  <verify>python -c "from spectral_select.viewer import ViewerApp; import tkinter as tk; root = tk.Tk(); app = ViewerApp(root); root.destroy(); print('Init OK')"</verify>
  <done>ViewerApp can load SpectraData from .pkl, populate excitation selector, display image with auto-contrast</done>
</task>

<task type="auto">
  <name>Task 3: Add display controls and mouse interaction</name>
  <files>spectral_select/viewer.py, spectral_select/__init__.py</files>
  <action>
Add display controls and basic interactivity:

1. **Display controls** (in Control Panel "Display" section):
   - Auto-contrast checkbox (default: checked)
   - Min/Max sliders or entry fields for manual contrast
   - RGB method selector: "RGB (3-band)", "Mean projection", "Max projection"
   - Apply button (or auto-apply on change)

2. **Mouse interaction**:
   - motion_notify_event: Update info bar with cursor position and pixel value
   - Format pixel value appropriately (scientific notation for small values)
   - Show "Outside image" when cursor leaves canvas

3. **Keyboard shortcuts**:
   - Ctrl+O: Open file
   - R: Reset view (zoom to fit)
   - Escape: Cancel current operation (future use)

4. **Helper functions** (module-level for testability):
   - create_rgb_image(cube, method='rgb', percentile=98) -> np.ndarray
   - get_pixel_value(cube, x, y, band=None) -> float or np.ndarray
   - detect_cube_format(cube) -> tuple of (bands_dim, height_dim, width_dim)

5. **Export from package**:
   - Add ViewerApp to spectral_select/__init__.py exports
   - Add launch_viewer() convenience function

6. **Status bar updates**:
   - "Ready" when idle
   - "Loading {filename}..." during load
   - "Loaded: {filename} ({n} excitations, {h}x{w})" after load
  </action>
  <verify>python -c "from spectral_select import ViewerApp; from spectral_select.viewer import create_rgb_image, detect_cube_format; print('All exports OK')"</verify>
  <done>Display controls working, mouse shows position/value, helper functions exported, ViewerApp in package __init__</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from spectral_select import ViewerApp"` succeeds
- [ ] ViewerApp window opens without errors
- [ ] Can load a SpectraData .pkl file via Open dialog
- [ ] Excitation selector populates and switches display
- [ ] Mouse position/value updates in info bar
- [ ] Display controls (contrast, RGB method) affect visualization
</verification>

<success_criteria>
- All tasks completed
- ViewerApp provides professional-looking window layout
- Data loading works for SpectraData .pkl files
- Basic image display with contrast controls
- Mouse interaction shows position and pixel values
- Helper functions exported for testability
</success_criteria>

<output>
After completion, create `.planning/phases/13-masking-gui-tool/13-01-SUMMARY.md`
</output>
