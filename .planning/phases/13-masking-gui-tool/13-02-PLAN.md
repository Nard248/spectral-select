---
phase: 13-masking-gui-tool
plan: 02
type: execute
---

<objective>
Add band browser slider and false color composer to enable detailed spectral exploration.

Purpose: Allow users to browse individual emission bands and create custom RGB visualizations by assigning any bands to color channels.
Output: Band slider control, false color composer panel, improved spectral navigation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Built in previous plan:
@spectral_select/viewer.py

# Reference for emission wavelengths:
@spectral_select/types.py

**Prior plan:** 13-01 created ViewerApp with window layout, data loading, basic display
**Key data:** ExcitationData.cube shape is (bands, height, width), ExcitationData.emission_wavelengths is list of wavelengths
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add emission band browser with slider</name>
  <files>spectral_select/viewer.py</files>
  <action>
Add band browsing capability to ViewerApp:

1. **Band Browser section** (in Control Panel after "Display"):
   - Label: "Band Browser"
   - Slider (ttk.Scale): Range 0 to num_bands-1
   - Band index spinbox: Direct entry with validation
   - Wavelength label: Shows emission wavelength for current band (e.g., "450.5 nm")
   - Play/Stop buttons: Auto-advance through bands (animation)

2. **Display mode toggle**:
   - Radio buttons: "Composite" vs "Single Band"
   - Composite: RGB visualization (existing behavior)
   - Single Band: Grayscale image of selected band only

3. **Band slider behavior**:
   - Moving slider updates display in real-time
   - Show band index and wavelength in info area
   - Colormap selector for single-band mode: viridis, grayscale, hot, cool

4. **Animation feature**:
   - Play button: Start timer advancing band every 100ms
   - Stop button: Halt animation
   - Speed slider: 50ms to 500ms interval
   - Loop checkbox: Restart from 0 after last band

5. **State management**:
   - self.current_band: int (current band index)
   - self.display_mode: str ('composite' or 'single')
   - self.animation_running: bool
   - self.colormap: str

6. **Update display logic**:
   - In single-band mode: display cube[band_idx, :, :] with colormap
   - Preserve contrast settings
   - Update info bar with band info
  </action>
  <verify>python -c "from spectral_select.viewer import ViewerApp; print('Band browser added')"</verify>
  <done>Band slider allows browsing individual bands, animation plays through bands, single-band mode with colormap selection</done>
</task>

<task type="auto">
  <name>Task 2: Implement false color composer</name>
  <files>spectral_select/viewer.py</files>
  <action>
Add false color composition panel:

1. **False Color section** (collapsible LabelFrame):
   - Checkbox: "Enable False Color" (toggles between auto-RGB and custom)
   - Three rows for R, G, B channel assignment:
     ```
     R: [Band dropdown ▼] [Wavelength label]
     G: [Band dropdown ▼] [Wavelength label]
     B: [Band dropdown ▼] [Wavelength label]
     ```
   - Each dropdown lists: band index + wavelength (e.g., "15: 520.0 nm")
   - "Apply" button to update display
   - "Reset to Default" button (auto-selects 20%, 50%, 80% bands)

2. **Presets dropdown**:
   - "Default (20/50/80%)" - spread across spectrum
   - "Chlorophyll" - bands sensitive to vegetation (if applicable)
   - "Custom" - user's current selection
   - Store presets in class constant or config

3. **Band selection logic**:
   - Populate dropdowns with all available bands + wavelengths
   - Default selection: indices at 20%, 50%, 80% of band count
   - Validate selections (warn if same band for multiple channels)

4. **Compose function** (module-level for testing):
   ```python
   def compose_false_color(cube: np.ndarray, r_band: int, g_band: int, b_band: int, percentile: float = 98) -> np.ndarray:
       """Create RGB image from specified bands."""
   ```

5. **Live preview option**:
   - Checkbox: "Live preview"
   - When enabled, changing any dropdown immediately updates display
   - When disabled, only "Apply" button updates

6. **Integration with display pipeline**:
   - When false color enabled, use compose_false_color() instead of create_rgb_image()
   - Store selected bands in state for persistence
  </action>
  <verify>python -c "from spectral_select.viewer import compose_false_color; import numpy as np; cube = np.random.rand(50, 100, 100); rgb = compose_false_color(cube, 10, 25, 40); print(f'Shape: {rgb.shape}')"</verify>
  <done>False color composer allows custom RGB band assignment with presets and live preview</done>
</task>

<task type="auto">
  <name>Task 3: Add zoom improvements and navigation</name>
  <files>spectral_select/viewer.py</files>
  <action>
Improve zoom and navigation experience:

1. **Zoom controls** (in toolbar or Display section):
   - Zoom in button (+)
   - Zoom out button (-)
   - Fit to window button
   - 1:1 (actual pixels) button
   - Current zoom level indicator (e.g., "150%")

2. **Mouse wheel zoom**:
   - Connect scroll_event to zoom in/out
   - Zoom centered on cursor position
   - Smooth zoom steps (1.2x per scroll)

3. **Pan with middle mouse**:
   - Middle-click and drag to pan
   - Or hold Space + left-click drag

4. **Minimap/overview** (optional, if time permits):
   - Small thumbnail in corner showing full image
   - Rectangle indicating current viewport
   - Click minimap to jump to location

5. **Coordinate display improvements**:
   - Show both pixel coordinates and physical coordinates if available
   - Format: "Pixel: (152, 89) | Band 25: 520nm | Value: 0.0234"

6. **View state persistence**:
   - Remember zoom/pan when switching excitations
   - Reset view button clears to fit-to-window

7. **Performance optimization**:
   - Only redraw changed elements
   - Use blitting for faster updates during pan/zoom
   - Debounce slider updates during rapid movement
  </action>
  <verify>python -c "from spectral_select.viewer import ViewerApp; print('Zoom improvements complete')"</verify>
  <done>Mouse wheel zoom, improved pan controls, zoom level indicator, smooth navigation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Band slider browses through all emission bands
- [ ] Single-band mode displays individual band with colormap
- [ ] Animation plays through bands smoothly
- [ ] False color composer creates custom RGB from any 3 bands
- [ ] Mouse wheel zooms centered on cursor
- [ ] Zoom level displayed, fit/1:1 buttons work
</verification>

<success_criteria>
- All tasks completed
- Band browser provides smooth navigation through spectrum
- False color composer enables custom RGB visualization
- Zoom/pan navigation feels responsive and intuitive
- compose_false_color() function exported for testing
</success_criteria>

<output>
After completion, create `.planning/phases/13-masking-gui-tool/13-02-SUMMARY.md`
</output>
