---
phase: 03-core-data-types
plan: 02
type: execute
---

<objective>
Create data classes for representing wavelength selection analysis results.

Purpose: Replace ad-hoc dictionary structures for analysis output with typed, validated data classes that capture selected wavelength bands, analysis metadata, and performance metrics.
Output: WavelengthBand, WavelengthResult, and AnalysisMetrics classes in spectral_select/types.py
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-data-types/03-01-PLAN.md

# Key source files showing current result structures:
@wavelength_analysis/core/analyzer.py (lines 500-700 for selected_bands structure)

# Current types.py (will contain input types from 03-01):
@spectral_select/types.py

# Patterns from Phase 2:
@spectral_select/config.py

**Tech stack available:** dataclasses, numpy, pathlib, datetime
**Established patterns:** dataclass with __post_init__ validation, to_dict/from_dict serialization
**Constraining decisions:**
- Follow same serialization patterns as Config and input types
- Results must be JSON-serializable for saving
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WavelengthBand dataclass</name>
  <files>spectral_select/types.py</files>
  <action>
Create a WavelengthBand dataclass for a single selected wavelength combination:
- rank: int (1-indexed rank in selection order)
- excitation_nm: float (excitation wavelength in nanometers)
- emission_nm: float (emission wavelength in nanometers)
- emission_band_index: int (index into the emission band array)
- influence_score: float (importance/sensitivity score)

Add __post_init__ validation:
- rank must be >= 1
- excitation_nm must be positive
- emission_nm must be positive
- emission_band_index must be >= 0

Methods:
- to_dict() -> Dict[str, Any] (for JSON serialization)
- from_dict(data: Dict[str, Any]) -> "WavelengthBand" (class method for deserialization)

Add __repr__ for readable output: "WavelengthBand(rank=1, ex=365.0nm, em=500.0nm, score=0.85)"
  </action>
  <verify>python -c "from spectral_select.types import WavelengthBand; wb = WavelengthBand(rank=1, excitation_nm=365.0, emission_nm=500.0, emission_band_index=8, influence_score=0.85); print(wb); print(wb.to_dict())"</verify>
  <done>WavelengthBand dataclass with all fields, validation, to_dict, from_dict, and custom __repr__</done>
</task>

<task type="auto">
  <name>Task 2: Create AnalysisMetrics dataclass</name>
  <files>spectral_select/types.py</files>
  <action>
Create an AnalysisMetrics dataclass for analysis performance metrics:
- total_bands_available: int (total number of wavelength combinations in input)
- bands_selected: int (number of bands selected)
- compression_ratio: float (total_bands / bands_selected)
- max_influence_score: float (highest influence score)
- min_influence_score: float (lowest influence score among selected)
- mean_influence_score: float (average influence score)

Add __post_init__ validation:
- All counts must be positive
- compression_ratio must be >= 1.0
- min <= mean <= max for influence scores

Methods:
- to_dict() -> Dict[str, Any]
- from_dict(data: Dict[str, Any]) -> "AnalysisMetrics" (class method)

Class method for convenience:
- from_bands(bands: List[WavelengthBand], total_available: int) -> "AnalysisMetrics"
  Compute all metrics from a list of selected bands
  </action>
  <verify>python -c "from spectral_select.types import WavelengthBand, AnalysisMetrics; bands = [WavelengthBand(1, 365, 500, 8, 0.9), WavelengthBand(2, 365, 510, 9, 0.7)]; m = AnalysisMetrics.from_bands(bands, 1000); print(m)"</verify>
  <done>AnalysisMetrics dataclass with all fields, validation, to_dict, from_dict, and from_bands factory</done>
</task>

<task type="auto">
  <name>Task 3: Create WavelengthResult dataclass</name>
  <files>spectral_select/types.py</files>
  <action>
Create a WavelengthResult dataclass as the complete analysis output container:
- sample_name: str (identifier for the analyzed sample)
- selected_bands: List[WavelengthBand] (ordered list of selected wavelength combinations)
- metrics: AnalysisMetrics (performance metrics)
- timestamp: datetime = field(default_factory=datetime.now) (when analysis was run)
- config_snapshot: Optional[Dict[str, Any]] = None (Config.to_dict() at time of analysis)
- method_summary: Dict[str, str] = field(default_factory=dict) (dimension_selection, perturbation, normalization methods used)

Properties:
- n_bands: int (len(selected_bands))
- top_band: WavelengthBand (first/highest-ranked band)
- excitation_wavelengths: List[float] (unique excitations in selected bands)

Methods:
- to_dict() -> Dict[str, Any] (full serialization for JSON)
- from_dict(data: Dict[str, Any]) -> "WavelengthResult" (class method)
- to_json(path: Union[str, Path]) -> None (save to JSON file)
- from_json(path: Union[str, Path]) -> "WavelengthResult" (class method to load)
- get_bands_for_excitation(ex_nm: float) -> List[WavelengthBand] (filter by excitation)

Add __post_init__:
- Ensure selected_bands is sorted by rank
- Validate ranks are sequential starting from 1

Update spectral_select/__init__.py to export the new types:
- Add WavelengthBand, WavelengthResult, AnalysisMetrics, SpectraData, ExcitationData, LoadingOptions to __all__
  </action>
  <verify>python -c "from spectral_select.types import WavelengthBand, WavelengthResult, AnalysisMetrics; bands = [WavelengthBand(1, 365, 500, 8, 0.9)]; m = AnalysisMetrics.from_bands(bands, 100); r = WavelengthResult('test', bands, m); print(r.n_bands, r.top_band)"</verify>
  <done>WavelengthResult dataclass with all fields, properties, serialization methods, and package exports updated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from spectral_select.types import WavelengthBand, AnalysisMetrics, WavelengthResult"` succeeds
- [ ] `python -c "from spectral_select import WavelengthResult"` succeeds (exported at package level)
- [ ] WavelengthResult can be serialized to JSON and loaded back
- [ ] All classes have docstrings
- [ ] No import errors in spectral_select package
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No import errors in spectral_select package
- WavelengthBand matches the structure used in analyzer.py selected_bands
- WavelengthResult can replace the ad-hoc dict returned by run_complete_analysis()
- AnalysisMetrics captures all metrics from save_results()
- Phase 3 complete - all core data types implemented
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-data-types/03-02-SUMMARY.md`

Include in summary:
- Phase 3 complete
- Ready for Phase 4: Analysis Engine
</output>
