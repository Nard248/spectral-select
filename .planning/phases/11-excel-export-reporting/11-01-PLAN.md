---
phase: 11-excel-export-reporting
plan: 01
type: execute
---

<objective>
Add Excel export capability to WavelengthResult for simple flat table output of selected wavelengths.

Purpose: Enable researchers to export wavelength selection results to Excel format for analysis, sharing, and integration with other tools.
Output: WavelengthResult.to_excel() method producing flat table with Rank, Excitation_nm, Emission_nm, Band_Index, Score columns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Auto-selected based on dependency graph:
@.planning/phases/03-core-data-types/03-02-SUMMARY.md
@.planning/phases/10-results-organization/10-02-SUMMARY.md

# Key files:
@spectral_select/types.py
@pyproject.toml

**Tech stack available:** pandas (already in dependencies), numpy
**Established patterns:** to_json()/from_json() serialization pattern in WavelengthResult
**Constraining decisions:**
- [03-02]: JSON serialization for results (human-readable, portable) - Excel is complementary
- [03-02]: WavelengthBand has rank, excitation_nm, emission_nm, emission_band_index, influence_score fields
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add openpyxl dependency for Excel support</name>
  <files>pyproject.toml</files>
  <action>Add openpyxl>=3.1 to the dependencies list in pyproject.toml. This is the engine pandas uses for .xlsx format. Place it alphabetically in the dependencies list (after numpy, before pandas).</action>
  <verify>grep "openpyxl" pyproject.toml shows the dependency</verify>
  <done>openpyxl>=3.1 appears in the dependencies list in pyproject.toml</done>
</task>

<task type="auto">
  <name>Task 2: Add WavelengthResult.to_excel() method</name>
  <files>spectral_select/types.py</files>
  <action>
Add a to_excel() method to the WavelengthResult class following the pattern of to_json():

1. Import pandas at the top of the file (add to existing imports)
2. Add method to_excel(self, path: Union[str, Path], include_metrics: bool = True) -> None:
   - Create DataFrame from selected_bands with columns: Rank, Excitation_nm, Emission_nm, Band_Index, Score
   - Optionally add a second sheet "Metrics" with AnalysisMetrics data if include_metrics=True
   - Use pandas to_excel() with openpyxl engine
   - Create parent directories if needed (like to_json does)
   - Add docstring following existing patterns

Column mapping from WavelengthBand:
- Rank: band.rank
- Excitation_nm: band.excitation_nm
- Emission_nm: band.emission_nm
- Band_Index: band.emission_band_index
- Score: band.influence_score

For metrics sheet (if include_metrics=True), create single-row DataFrame with:
- Total_Bands, Bands_Selected, Compression_Ratio, Max_Score, Min_Score, Mean_Score
</action>
  <verify>python -c "from spectral_select.types import WavelengthResult; print(hasattr(WavelengthResult, 'to_excel'))" returns True</verify>
  <done>WavelengthResult has to_excel() method that exports wavelength bands to Excel with optional metrics sheet</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] openpyxl is in pyproject.toml dependencies
- [ ] WavelengthResult.to_excel() method exists
- [ ] No import errors: `python -c "from spectral_select import WavelengthResult"`
- [ ] All existing tests pass: `pytest tests/ -q`
</verification>

<success_criteria>
- openpyxl dependency added to pyproject.toml
- WavelengthResult.to_excel() method implemented
- Method creates Excel file with "Wavelengths" sheet containing flat table
- Optional "Metrics" sheet with analysis metrics
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-excel-export-reporting/11-01-SUMMARY.md`
</output>
