---
phase: 11-excel-export-reporting
plan: 02
type: execute
---

<objective>
Add ResultsManager integration for Excel exports and comprehensive tests for Excel export functionality.

Purpose: Provide convenient export paths through ResultsManager and ensure Excel export works correctly through automated tests.
Output: ResultsManager.get_export_path() helper, comprehensive test suite for Excel export.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Auto-selected based on dependency graph:
@.planning/phases/10-results-organization/10-01-SUMMARY.md
@.planning/phases/11-excel-export-reporting/11-01-SUMMARY.md

# Key files:
@spectral_select/results.py
@spectral_select/types.py
@tests/test_results.py
@tests/test_types.py

**Tech stack available:** pytest, pandas, openpyxl (added in 11-01)
**Established patterns:**
- ResultsManager path getters (get_model_path, get_result_path, get_viz_path)
- Test class naming: Test{ClassName}{Aspect}
**Constraining decisions:**
- [10-01]: ResultsManager uses run_dir as base for result files
- [08-01]: Function scope for test fixtures (mutable data)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ResultsManager export path helper</name>
  <files>spectral_select/results.py</files>
  <action>
Add get_export_path() method to ResultsManager following the pattern of get_result_path():

def get_export_path(self, filename: str) -> Path:
    """Get path for an export file in the run directory.

    This is an alias for get_result_path() provided for semantic clarity
    when working with export files like Excel spreadsheets.

    Args:
        filename: Name of the export file (e.g., "wavelength_selection.xlsx").

    Returns:
        Full path to the export file.
    """
    return self.run_dir / filename

Also add a convenience constant at class level for the default export filename:
DEFAULT_EXPORT_FILENAME = "wavelength_selection.xlsx"
</action>
  <verify>python -c "from spectral_select.results import ResultsManager; print(hasattr(ResultsManager, 'get_export_path'))" returns True</verify>
  <done>ResultsManager.get_export_path() method exists and returns path in run_dir</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for Excel export functionality</name>
  <files>tests/test_types.py</files>
  <action>
Add a new test class TestWavelengthResultExcel with tests for the to_excel() method:

1. test_to_excel_creates_file: Verify file is created at specified path
2. test_to_excel_wavelengths_sheet: Read back Excel and verify Wavelengths sheet has correct columns and data
3. test_to_excel_metrics_sheet: Verify Metrics sheet is present and has correct values when include_metrics=True
4. test_to_excel_no_metrics: Verify only Wavelengths sheet when include_metrics=False
5. test_to_excel_creates_parent_dirs: Verify parent directories are created if needed

Use tmp_path fixture for temporary file paths.
Create a simple WavelengthResult fixture with 3-5 bands for testing.
Use pandas.read_excel() to read back and verify data.

Add at least 5 tests total.
</action>
  <verify>pytest tests/test_types.py::TestWavelengthResultExcel -v shows all tests pass</verify>
  <done>At least 5 tests for Excel export, all passing</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP to mark Phase 11 complete</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update .planning/ROADMAP.md:
1. Change Phase 11 checkbox from [ ] to [x]
2. Add **Completed**: date field
3. Update Plans section to show completed plans:
   - [x] 11-01: Excel export core ✓
   - [x] 11-02: ResultsManager integration and tests ✓
4. Update Progress table: change "0/?" to "2/2" and status to "Complete"
</action>
  <verify>grep "Phase 11" .planning/ROADMAP.md shows [x] checkbox</verify>
  <done>ROADMAP.md shows Phase 11 as complete with both plans marked done</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ResultsManager.get_export_path() method exists
- [ ] At least 5 Excel export tests exist and pass
- [ ] ROADMAP.md shows Phase 11 complete
- [ ] All tests pass: `pytest tests/ -q`
- [ ] Total test count increased
</verification>

<success_criteria>
- ResultsManager has get_export_path() method
- TestWavelengthResultExcel class with 5+ tests
- All tests pass
- ROADMAP.md updated with Phase 11 completion
- Phase 11 complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-excel-export-reporting/11-02-SUMMARY.md`
</output>
